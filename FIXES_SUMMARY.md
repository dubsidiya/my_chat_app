# Исправления: создатель чата, WebSocket и удаление участников

## ✅ Что было исправлено:

### 1. **Определение создателя чата**

#### Проблема:
- Создатель определялся неправильно (кто открывал диалог)
- Можно было удалить создателя чата

#### Решение:
- Добавлено поле `created_by` в таблицу `chats`
- При создании чата первый пользователь в списке `userIds` становится создателем
- Создатель защищен от удаления на сервере
- В диалоге участников показывается метка "Создатель чата"

#### SQL для выполнения в Neon:
```sql
-- Добавляем колонку created_by
ALTER TABLE chats ADD COLUMN IF NOT EXISTS created_by INTEGER REFERENCES users(id) ON DELETE SET NULL;

-- Обновляем существующие чаты: устанавливаем created_by как первого участника
UPDATE chats c
SET created_by = (
  SELECT cu.user_id 
  FROM chat_users cu 
  WHERE cu.chat_id = c.id 
  ORDER BY cu.chat_id 
  LIMIT 1
)
WHERE created_by IS NULL;
```

### 2. **WebSocket для реального времени**

#### Проблема:
- Сообщения не обновлялись в реальном времени

#### Решение:
- После отправки сообщения через HTTP, оно автоматически отправляется через WebSocket всем участникам чата
- WebSocket подключение теперь передает `userId` в query параметре
- Сообщения появляются мгновенно у всех участников

### 3. **Ошибка 500 при удалении участника**

#### Проблема:
- Ошибка 500 при удалении участника из группового чата

#### Решение:
- Исправлена логика удаления участника
- Добавлена проверка на создателя чата
- Исправлен роут: `DELETE /chats/:id/members/:userId` (userId в URL, а не в body)

## 📋 Изменения в коде:

### Сервер:

1. **`controllers/chatsController.js`**:
   - `createChat`: сохраняет `created_by` при создании чата
   - `getChatMembers`: возвращает `is_creator` для каждого участника
   - `removeMemberFromChat`: защищает создателя от удаления

2. **`controllers/messagesController.js`**:
   - `sendMessage`: отправляет сообщение через WebSocket после сохранения в БД

3. **`websocket/websocket.js`**:
   - Экспортирует функцию `getWebSocketClients()` для доступа к клиентам

4. **`routes/chats.js`**:
   - Роут удаления: `DELETE /chats/:id/members/:userId`

### Приложение:

1. **`lib/screens/chat_screen.dart`**:
   - WebSocket подключение с `userId` в query параметре

2. **`lib/screens/chat_members_dialog.dart`**:
   - Использует `is_creator` из ответа сервера
   - Показывает метку "Создатель чата" для создателя
   - Не позволяет удалить создателя

## 🚀 Что нужно сделать:

1. **Выполните SQL в Neon**:
   - Откройте Neon SQL Editor
   - Выполните SQL из файла `add_created_by_column.sql`

2. **Загрузите обновленные файлы на Render**:
   - `controllers/chatsController.js`
   - `controllers/messagesController.js`
   - `websocket/websocket.js`
   - `routes/chats.js`

3. **Перезапустите сервер на Render**

4. **Протестируйте**:
   - Создайте чат - вы будете создателем
   - Добавьте участников
   - Попробуйте удалить создателя - должно быть запрещено
   - Отправьте сообщение - оно должно появиться в реальном времени у всех участников

## ✅ Результат:

- ✅ Создатель чата определяется правильно
- ✅ Создатель защищен от удаления
- ✅ Сообщения обновляются в реальном времени
- ✅ Ошибка 500 при удалении исправлена
- ✅ Все участники видят метку создателя

