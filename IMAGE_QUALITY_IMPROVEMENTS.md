# 🎨 Улучшение качества изображений и скачивание оригинала

## Внесенные изменения

### 1. ✅ Улучшено качество сжатия

**Параметры сжатия:**
- **maxWidth**: 1920px → **2560px** (больше разрешение)
- **quality**: 85% → **92%** (выше качество JPEG)

**Результат:**
- Изображения сохраняют больше деталей
- Меньше артефактов сжатия
- Все еще экономия памяти (30-50% вместо 50-80%)

### 2. ✅ Сохранение оригинального изображения

**Как работает:**
1. При загрузке изображения отправляются **два файла**:
   - `image` - сжатое изображение для отображения (2560px, quality 92%)
   - `original` - оригинальное изображение без изменений

2. Оригинал сохраняется в папке `original/` в Яндекс Object Storage
3. Сжатое изображение сохраняется в папке `images/` (как раньше)

**Структура в бакете:**
```
my-chat-images/
  ├── images/
  │   └── image-xxx.jpg (сжатое)
  └── original/
      └── original-image-xxx.jpg (оригинал)
```

### 3. ✅ Кнопка скачивания оригинала

**Где находится:**
- В полноэкранном просмотре изображения (диалог)
- Кнопка с иконкой скачивания рядом с кнопкой закрытия

**Как работает:**
- На веб: скачивает изображение через браузер
- На мобильных: будет добавлено позже

### 4. ✅ Обновлена модель Message

Добавлено поле `originalImageUrl` для хранения URL оригинального изображения.

## Технические детали

### Серверные изменения

1. **uploadImage endpoint** (`/messages/upload-image`):
   - Принимает до 2 файлов: `image` (сжатое) и `original` (оригинал)
   - Возвращает `image_url` и `original_image_url`

2. **yandexStorage.js**:
   - Функция `uploadToYandex` теперь принимает параметр `folder`
   - Оригиналы сохраняются в папке `original/`

3. **uploadImage.js**:
   - Multer настроен на прием до 2 файлов
   - Лимит размера увеличен до 10MB для оригинала

### Клиентские изменения

1. **chat_screen.dart**:
   - Функция `_compressImage` с улучшенными параметрами
   - Отправка оригинала вместе со сжатым изображением
   - Функция `_downloadImage` для скачивания

2. **messages_service.dart**:
   - `uploadImage` принимает `originalBytes` опционально
   - Отправляет оригинал, если он есть

3. **message.dart**:
   - Добавлено поле `originalImageUrl`
   - Добавлен геттер `hasOriginalImage`

## Использование

### Для пользователя

1. **Загрузка изображения:**
   - Выберите изображение как обычно
   - Оригинал автоматически сохраняется на сервере

2. **Скачивание оригинала:**
   - Откройте изображение в полноэкранном режиме
   - Нажмите кнопку скачивания (иконка загрузки)
   - Изображение скачается в оригинальном качестве

### Для разработчика

**Если нужно изменить качество сжатия:**

```dart
// В chat_screen.dart, функция _compressImage
Future<Uint8List> _compressImage(Uint8List imageBytes, {
  int maxWidth = 2560,  // Измените здесь
  int quality = 92      // И здесь
}) async {
  // ...
}
```

## База данных (опционально)

Если вы хотите сохранять `original_image_url` в базе данных для доступа позже, добавьте поле:

```sql
ALTER TABLE messages ADD COLUMN original_image_url TEXT;
```

Затем обновите `sendMessage` в `messagesController.js` для сохранения `original_image_url`.

**Примечание:** Это не обязательно - оригинал доступен через API при загрузке, и можно скачать его сразу.

## Обратная совместимость

- Старые изображения (без оригинала) продолжают работать
- Если оригинала нет, кнопка скачивания скачивает сжатое изображение
- Все существующие функции работают как раньше

## Экономия памяти

**До изменений:**
- Сжатие: 50-80% экономии
- Качество: 85%

**После изменений:**
- Сжатие: 30-50% экономии (меньше, но лучше качество)
- Качество: 92% (значительно лучше)
- Оригинал доступен для скачивания

## Рекомендации

1. **Для фотографий:** Используйте качество 92% (текущее)
2. **Для скриншотов/графики:** Можно снизить до 85% для большей экономии
3. **Для важных изображений:** Всегда скачивайте оригинал, если нужен

