# Функциональность добавления участников в чат

## ✅ Что было добавлено:

### 1. **Сервер (Backend)**

#### Новые контроллеры:
- **`getAllUsers`** в `authController.js` - получение списка всех пользователей
- **`getChatMembers`** в `chatsController.js` - получение участников чата
- **`addMembersToChat`** в `chatsController.js` - добавление участников в чат

#### Новые роуты:
- `GET /auth/users` - получение всех пользователей
- `GET /chats/:id/members` - получение участников чата
- `POST /chats/:id/members` - добавление участников в чат

### 2. **Приложение (Frontend)**

#### Новые методы в ChatsService:
- **`getAllUsers(String excludeUserId)`** - получение всех пользователей (кроме текущего)
- **`getChatMembers(String chatId)`** - получение участников чата
- **`addMembersToChat(String chatId, List<String> userIds)`** - добавление участников

#### Новый UI:
- **Кнопка добавления участников** в AppBar экрана чата (иконка `person_add`)
- **Диалог выбора пользователей** (`AddMembersDialog`) с чекбоксами
- Автоматическая фильтрация - показываются только пользователи, которых еще нет в чате

## 🎯 Как использовать:

1. **Откройте чат**
2. **Нажмите на иконку** `person_add` в правом верхнем углу
3. **Выберите пользователей** из списка (можно выбрать несколько)
4. **Нажмите "Добавить"** - участники будут добавлены в чат

## 🔧 Что происходит на сервере:

1. Проверяется существование чата
2. Проверяется, не является ли пользователь уже участником (избегаем дубликатов)
3. Добавляются новые участники в таблицу `chat_users`
4. Если участников стало больше 1, чат автоматически становится групповым (`is_group = true`)

## 📋 Особенности:

- ✅ Показываются только пользователи, которых еще нет в чате
- ✅ Можно выбрать несколько пользователей одновременно
- ✅ Автоматическое обновление типа чата (личный → групповой)
- ✅ Защита от дубликатов - пользователь не добавится дважды
- ✅ Показывается количество выбранных пользователей в кнопке

## 🚀 Готово к использованию!

Все файлы обновлены и готовы к работе. Просто загрузите обновленные файлы на сервер и перезапустите его.

