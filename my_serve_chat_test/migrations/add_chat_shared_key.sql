-- Упрощённая модель E2EE: вместо пер-юзерных копий ключа чата сервер хранит один общий
-- AES-256 ключ (base64) на чат и выдаёт его любому участнику. Старый механизм chat_keys
-- остаётся как fallback для уже существующих чатов, поэтому миграция аддитивная.

ALTER TABLE chats
ADD COLUMN IF NOT EXISTS shared_chat_key TEXT;
