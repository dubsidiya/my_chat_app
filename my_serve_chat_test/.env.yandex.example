# Пример для подключения к Yandex Managed PostgreSQL (файл с реальными данными — .env.yandex — в .gitignore)
# После создания кластера скопируй сюда хост, пароль и сохрани как .env.yandex (не коммитить).
# DATABASE_URL=postgresql://chat_app:PASSWORD@HOST:6432/chat_db?sslmode=require
