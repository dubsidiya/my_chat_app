# 🔧 Настройка CORS для Яндекс Object Storage

## Автоматическая настройка

Я создал скрипт для автоматической настройки CORS. Выполните:

### Локально (если есть доступ к переменным окружения):

```bash
cd my_serve_chat_test
npm run setup-cors
```

### На Render.com (через SSH или через скрипт):

Если у вас есть доступ к серверу через SSH:

```bash
cd my_serve_chat_test
npm run setup-cors
```

## Что делает скрипт

Скрипт `utils/setupCors.js` автоматически:
1. ✅ Подключается к Яндекс Object Storage
2. ✅ Настраивает CORS правила:
   - Разрешенные источники: `*` (все)
   - Разрешенные методы: `GET, HEAD, OPTIONS`
   - Разрешенные заголовки: `*` (все)
   - Максимальный возраст: `3600` секунд
3. ✅ Применяет настройки к вашему бакету

## Требования

Убедитесь, что на сервере (или локально) установлены переменные окружения:

```env
YANDEX_ACCESS_KEY_ID=ваш_access_key_id
YANDEX_SECRET_ACCESS_KEY=ваш_secret_access_key
YANDEX_BUCKET_NAME=mychatimage
```

## Альтернатива: Настройка через консоль Яндекс Облака

Если скрипт не работает, настройте CORS вручную:

1. Откройте https://console.cloud.yandex.ru/
2. Перейдите в **Object Storage** → ваш бакет (`mychatimage`)
3. Перейдите на вкладку **"CORS"**
4. Нажмите **"Настроить CORS"**
5. Добавьте правило:
   - **Разрешенные источники**: `*`
   - **Разрешенные методы**: `GET, HEAD, OPTIONS`
   - **Разрешенные заголовки**: `*`
   - **Максимальный возраст**: `3600`
6. Нажмите **"Сохранить"**

## После настройки

1. Обновите страницу в браузере (Ctrl+F5 или Cmd+Shift+R)
2. Попробуйте загрузить новое изображение
3. Старые изображения должны начать отображаться

## Проверка

После настройки CORS:
- ✅ Изображения должны отображаться в чате
- ✅ В консоли браузера (F12) не должно быть ошибок CORS
- ✅ При клике на изображение должно открываться в полном размере

