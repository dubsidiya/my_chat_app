package com.example.my_chat_app

import android.app.Application

/**
 * Real Application class so the IDE resolves `android:name`.
 * Flutter's `${applicationName}` placeholder is only substituted at Gradle merge time.
 */
class MainApplication : Application()
