# 🔧 Исправление ошибки на Render.com

## Проблема

Ошибка при запуске на Render.com:
```
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '/opt/render/project/src/utils/yandexStorage.js'
```

## Причина

Файл `yandexStorage.js` существует в репозитории, но на Render.com он не находится по пути `/opt/render/project/src/utils/yandexStorage.js`.

## Решение

### Вариант 1: Проверить настройки Root Directory на Render.com

1. Перейдите в настройки вашего сервиса на Render.com
2. Проверьте поле **"Root Directory"**
3. Оно должно быть установлено как: `my_serve_chat_test`
4. Если установлено другое значение (например, `src`), измените на `my_serve_chat_test`

### Вариант 2: Убедиться, что файл отправлен в git

Если файл был создан недавно, убедитесь, что он закоммичен и отправлен:

```bash
# Проверить статус файла
git status my_serve_chat_test/utils/yandexStorage.js

# Если файл не закоммичен, добавить и закоммитить
git add my_serve_chat_test/utils/yandexStorage.js
git commit -m "Add yandexStorage.js for Yandex Object Storage"
git push
```

### Вариант 3: Проверить, что файл не игнорируется .gitignore

Убедитесь, что файл не игнорируется в `.gitignore`:

```bash
# Проверить, игнорируется ли файл
git check-ignore -v my_serve_chat_test/utils/yandexStorage.js
```

Если файл игнорируется, удалите соответствующее правило из `.gitignore`.

## Проверка

После исправления:

1. На Render.com нажмите **"Manual Deploy"** → **"Deploy latest commit"**
2. Проверьте логи - ошибка должна исчезнуть
3. Сервер должен запуститься успешно

## Если проблема сохраняется

1. Проверьте логи на Render.com - там будет точный путь, где ищется файл
2. Убедитесь, что структура папок на Render соответствует структуре в git
3. Возможно, нужно пересоздать сервис на Render.com с правильной Root Directory

