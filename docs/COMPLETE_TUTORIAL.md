# Полный учебник по Flutter Chat App
## От новичка к пониманию каждой строки кода

---

## Оглавление

1. [Введение в Flutter и Dart](#введение)
2. [Архитектура проекта](#архитектура)
3. [Основы Dart синтаксиса](#dart-синтаксис)
4. [Разбор каждого файла](#разбор-файлов)
5. [Паттерны и лучшие практики](#паттерны)
6. [Работа с API и WebSocket](#api-websocket)

---

## Введение в Flutter и Dart {#введение}

### Что такое Flutter?
Flutter - это фреймворк от Google для создания кроссплатформенных приложений (iOS, Android, Web, Desktop) из одного кода на языке Dart.

### Что такое Dart?
Dart - язык программирования, разработанный Google. Похож на JavaScript и Java, но имеет свои особенности.

### Основные концепции:
- **Widget** - основной строительный блок UI во Flutter. Всё является виджетом.
- **State** - состояние виджета, которое может изменяться.
- **Build** - метод, который строит UI на основе текущего состояния.

---

## Архитектура проекта {#архитектура}

```
lib/
├── main.dart              # Точка входа приложения
├── models/                # Модели данных (структуры)
│   ├── chat.dart
│   ├── message.dart
│   └── user.dart
├── screens/               # Экраны приложения (UI)
│   ├── login_screen.dart
│   ├── register_screen.dart
│   ├── home_screen.dart
│   ├── chat_screen.dart
│   ├── add_members_dialog.dart
│   └── chat_members_dialog.dart
└── services/              # Сервисы (бизнес-логика, API)
    ├── auth_service.dart
    ├── chats_service.dart
    ├── messages_service.dart
    └── storage_service.dart
```

### Паттерн архитектуры: MVC (Model-View-Controller)
- **Models** (`models/`) - структуры данных
- **Views** (`screens/`) - пользовательский интерфейс
- **Controllers/Services** (`services/`) - бизнес-логика и работа с API

---

## Основы Dart синтаксиса {#dart-синтаксис}

### 1. Переменные

```dart
// Неизменяемая переменная (константа времени компиляции)
final String name = "Иван";

// Изменяемая переменная
String email = "ivan@example.com";

// Переменная, которая может быть null
String? optionalName;  // ? означает, что значение может быть null

// Константа времени выполнения
const int maxUsers = 100;
```

**Разница между `final` и `const`:**
- `const` - значение известно во время компиляции (константа)
- `final` - значение устанавливается один раз во время выполнения

### 2. Типы данных

```dart
// Числа
int age = 25;                    // Целое число
double price = 99.99;           // Число с плавающей точкой

// Строки
String text = "Привет";         // Строка
String multiline = """
Многострочный
текст
""";

// Булево значение
bool isActive = true;

// Списки (массивы)
List<String> names = ["Иван", "Мария"];
List<int> numbers = [1, 2, 3];

// Мапы (объекты/словари)
Map<String, dynamic> user = {
  "id": "1",
  "email": "user@example.com"
};

// Динамический тип (может быть любым)
dynamic anything = "строка";
anything = 123;  // Теперь число
```

### 3. Функции

```dart
// Обычная функция
void printMessage(String message) {
  print(message);
}

// Функция с возвращаемым значением
int add(int a, int b) {
  return a + b;
}

// Стрелочная функция (короткая запись)
int multiply(int a, int b) => a * b;

// Асинхронная функция (возвращает Future)
Future<String> fetchData() async {
  // await - ждем завершения асинхронной операции
  final response = await http.get(url);
  return response.body;
}

// Функция с именованными параметрами
void greet({required String name, int age = 18}) {
  print("Привет, $name! Тебе $age лет");
}
// Вызов: greet(name: "Иван", age: 25);
```

### 4. Классы

```dart
class Person {
  // Поля класса
  final String name;
  int age;
  
  // Конструктор
  Person({required this.name, this.age = 0});
  
  // Метод
  void introduce() {
    print("Меня зовут $name, мне $age лет");
  }
  
  // Геттер
  String get info => "$name ($age лет)";
  
  // Сеттер
  set setAge(int newAge) {
    if (newAge > 0) {
      age = newAge;
    }
  }
}

// Использование
final person = Person(name: "Иван", age: 25);
person.introduce();
```

### 5. Null Safety (Безопасность null)

```dart
// Безопасный доступ к nullable значению
String? name;
print(name?.length);  // Если name == null, вернет null, не вызовет ошибку

// Оператор ! (assertion) - говорим, что уверены, что не null
String definitelyNotNull = name!;  // Опасно! Может вызвать ошибку

// Оператор ?? (null coalescing) - значение по умолчанию
String displayName = name ?? "Гость";  // Если name == null, вернет "Гость"

// Оператор ??= (null-aware assignment)
name ??= "По умолчанию";  // Присвоит только если name == null
```

### 6. Асинхронность (async/await)

```dart
// Future - обещание будущего значения
Future<String> getData() async {
  await Future.delayed(Duration(seconds: 2));  // Ждем 2 секунды
  return "Данные получены";
}

// Использование
getData().then((data) {
  print(data);  // Выполнится через 2 секунды
});

// Или с await (внутри async функции)
final data = await getData();
print(data);
```

---

## Разбор каждого файла {#разбор-файлов}

### 1. main.dart - Точка входа

```dart
import 'package:flutter/material.dart';
```
**Объяснение:** Импортируем основной пакет Flutter для работы с Material Design (дизайн от Google).

```dart
import 'package:flutter/foundation.dart';
```
**Объяснение:** Импортируем утилиты для проверки режима отладки (`kDebugMode`).

```dart
import 'dart:async';
```
**Объяснение:** Импортируем библиотеку для работы с асинхронностью (`runZonedGuarded`).

```dart
void main() {
```
**Объяснение:** 
- `void` - функция ничего не возвращает
- `main()` - точка входа в программу, выполняется первой

```dart
FlutterError.onError = (FlutterErrorDetails details) {
```
**Объяснение:**
- `FlutterError.onError` - глобальный обработчик ошибок Flutter
- `(FlutterErrorDetails details)` - анонимная функция (лямбда) с параметром `details`
- `=>` можно заменить на `{ }` для многострочного кода

```dart
FlutterError.presentError(details);
```
**Объяснение:** Показываем ошибку пользователю (красный экран в режиме разработки).

```dart
if (kDebugMode) {
```
**Объяснение:** 
- `if` - условие
- `kDebugMode` - константа, `true` только в режиме отладки (не в релизной версии)
- Это позволяет не показывать логи в продакшене

```dart
runZonedGuarded(() {
```
**Объяснение:**
- `runZonedGuarded` - создает зону выполнения с обработкой ошибок
- Первый параметр `() { }` - код для выполнения
- Второй параметр `(error, stack) { }` - обработчик необработанных ошибок

```dart
class MyApp extends StatelessWidget {
```
**Объяснение:**
- `class` - определение класса
- `MyApp` - имя класса
- `extends StatelessWidget` - наследуемся от `StatelessWidget` (виджет без состояния)
- `StatelessWidget` - виджет, который не меняется после создания

```dart
@override
Widget build(BuildContext context) {
```
**Объяснение:**
- `@override` - аннотация, указывает что переопределяем метод родителя
- `Widget` - возвращаемый тип (виджет)
- `build` - метод, который строит UI
- `BuildContext context` - контекст виджета (нужен для навигации, получения тем и т.д.)

```dart
return MaterialApp(
```
**Объяснение:**
- `MaterialApp` - корневой виджет приложения Material Design
- Содержит навигацию, темы, локализацию

```dart
title: 'Chat App',
```
**Объяснение:** Именованный параметр `title` - название приложения.

```dart
theme: ThemeData(primarySwatch: Colors.red),
```
**Объяснение:**
- `theme` - тема приложения
- `ThemeData` - класс с настройками темы
- `primarySwatch: Colors.red` - основной цвет приложения (красный)

```dart
home: FutureBuilder<Map<String, String>?>(
```
**Объяснение:**
- `home` - начальный экран приложения
- `FutureBuilder` - виджет, который строит UI на основе `Future`
- `<Map<String, String>?>` - тип данных, который вернет Future
  - `Map` - словарь/объект
  - `String, String` - ключ и значение строки
  - `?` - может быть null

```dart
future: StorageService.getUserData(),
```
**Объяснение:**
- `future` - асинхронная операция, результат которой мы ждем
- `StorageService.getUserData()` - статический метод, возвращает `Future`

```dart
builder: (context, snapshot) {
```
**Объяснение:**
- `builder` - функция, которая строит UI на основе `snapshot`
- `snapshot` - снимок состояния Future (загружается, загружено, ошибка)

```dart
if (snapshot.connectionState == ConnectionState.waiting) {
```
**Объяснение:**
- `snapshot.connectionState` - состояние подключения
- `ConnectionState.waiting` - данные еще загружаются
- Показываем индикатор загрузки

```dart
return Scaffold(
  body: Center(child: CircularProgressIndicator()),
);
```
**Объяснение:**
- `Scaffold` - базовый виджет для экрана (AppBar, body, и т.д.)
- `body` - основное содержимое экрана
- `Center` - центрирует дочерний виджет
- `CircularProgressIndicator()` - крутящийся индикатор загрузки

```dart
if (snapshot.hasData && snapshot.data != null) {
```
**Объяснение:**
- `snapshot.hasData` - есть ли данные
- `snapshot.data != null` - данные не null (дополнительная проверка)
- `&&` - логическое И (оба условия должны быть true)

```dart
final userData = snapshot.data!;
```
**Объяснение:**
- `final` - неизменяемая переменная
- `snapshot.data!` - оператор `!` говорит "я уверен, что это не null"
- Безопасно, т.к. мы проверили выше

```dart
return HomeScreen(
  userId: userData['id']!,
  userEmail: userData['email']!,
);
```
**Объяснение:**
- Создаем экран `HomeScreen` с параметрами
- `userData['id']` - получаем значение по ключу 'id' из Map
- `!` - уверены, что значение не null

---

### 2. models/message.dart - Модель сообщения

```dart
class Message {
```
**Объяснение:** Определение класса `Message` - структура данных для сообщения.

```dart
final String id;
```
**Объяснение:**
- `final` - поле нельзя изменить после создания объекта
- `String` - тип данных (строка)
- `id` - идентификатор сообщения

```dart
Message({
  required this.id,
```
**Объяснение:**
- Конструктор с именованными параметрами
- `required` - параметр обязателен
- `this.id` - присваиваем значение параметра полю `id`

```dart
factory Message.fromJson(Map<String, dynamic> json) {
```
**Объяснение:**
- `factory` - фабричный конструктор (может возвращать существующий объект или создавать новый)
- `fromJson` - создает объект из JSON (Map)
- `Map<String, dynamic>` - словарь, где ключи - строки, значения - любые типы

```dart
try {
```
**Объяснение:** Начало блока обработки ошибок. Если что-то пойдет не так, выполнится `catch`.

```dart
return Message(
  id: (json['id'] ?? '').toString(),
```
**Объяснение:**
- `json['id']` - получаем значение по ключу 'id'
- `??` - если значение null, используем '' (пустую строку)
- `.toString()` - преобразуем в строку (на случай если пришло число)

```dart
catch (e) {
  print('Error parsing Message from JSON: $e');
  rethrow;
}
```
**Объяснение:**
- `catch (e)` - ловим ошибку в переменную `e`
- `print()` - выводим в консоль
- `$e` - интерполяция строки (вставляем значение переменной)
- `rethrow` - пробрасываем ошибку дальше (не скрываем)

---

### 3. services/auth_service.dart - Сервис аутентификации

```dart
class AuthService {
```
**Объяснение:** Класс для работы с аутентификацией (вход, регистрация).

```dart
final String baseUrl = 'https://my-server-chat.onrender.com';
```
**Объяснение:**
- `final` - константа (нельзя изменить)
- Базовый URL сервера для всех запросов

```dart
Future<Map<String, dynamic>?> loginUser(String email, String password) async {
```
**Объяснение:**
- `Future<>` - асинхронная операция, возвращает результат в будущем
- `Map<String, dynamic>?` - может вернуть Map или null
- `async` - функция асинхронная (может использовать `await`)

```dart
final response = await http.post(
```
**Объяснение:**
- `final` - неизменяемая переменная
- `await` - ждем завершения HTTP запроса
- `http.post()` - отправляем POST запрос

```dart
Uri.parse('$baseUrl/auth/login'),
```
**Объяснение:**
- `Uri.parse()` - создаем URI из строки
- `$baseUrl` - интерполяция (подставляем значение переменной)
- Полный URL: `https://my-server-chat.onrender.com/auth/login`

```dart
headers: {'Content-Type': 'application/json'},
```
**Объяснение:**
- `headers` - HTTP заголовки запроса
- `'Content-Type': 'application/json'` - говорим серверу, что отправляем JSON

```dart
body: jsonEncode({'email': email, 'password': password}),
```
**Объяснение:**
- `body` - тело запроса
- `jsonEncode()` - преобразует Map в JSON строку
- `{'email': email, 'password': password}` - Map с данными

```dart
if (response.statusCode == 200) {
```
**Объяснение:**
- `response.statusCode` - HTTP код ответа
- `200` - успешный запрос (OK)

```dart
return jsonDecode(response.body);
```
**Объяснение:**
- `response.body` - тело ответа (JSON строка)
- `jsonDecode()` - преобразует JSON строку в Map
- `return` - возвращаем результат

```dart
} else if (response.statusCode == 500) {
```
**Объяснение:**
- `else if` - альтернативное условие
- `500` - ошибка сервера (Internal Server Error)

```dart
throw Exception('Неверный email или пароль');
```
**Объяснение:**
- `throw` - выбрасываем исключение (ошибку)
- `Exception()` - создаем объект исключения с сообщением
- Выполнение функции прервется, ошибка будет обработана в `catch`

```dart
} catch (e) {
  if (e is Exception) {
    rethrow;
  }
```
**Объяснение:**
- `catch` - ловим любые ошибки
- `e is Exception` - проверяем, является ли ошибка Exception
- `is` - оператор проверки типа
- `rethrow` - пробрасываем дальше, если это Exception

---

### 4. screens/login_screen.dart - Экран входа

```dart
class LoginScreen extends StatefulWidget {
```
**Объяснение:**
- `StatefulWidget` - виджет с состоянием (может изменяться)
- В отличие от `StatelessWidget`, может перерисовываться при изменении состояния

```dart
@override
_LoginScreenState createState() => _LoginScreenState();
```
**Объяснение:**
- `createState()` - создает объект состояния
- `=>` - стрелочная функция (короткая запись)
- `_LoginScreenState` - класс состояния (приватный, начинается с `_`)

```dart
class _LoginScreenState extends State<LoginScreen> {
```
**Объяснение:**
- `extends State<LoginScreen>` - наследуемся от State
- `<LoginScreen>` - указываем тип виджета

```dart
final _authService = AuthService();
```
**Объяснение:**
- `final` - константа (нельзя переназначить)
- `_authService` - приватное поле (начинается с `_`)
- `AuthService()` - создаем экземпляр сервиса

```dart
final _emailController = TextEditingController();
```
**Объяснение:**
- `TextEditingController` - контроллер для управления текстовым полем
- Позволяет читать/писать текст, слушать изменения

```dart
String? _errorMessage;
```
**Объяснение:**
- `String?` - строка, которая может быть null
- `_errorMessage` - приватное поле для хранения ошибки
- Изначально `null` (нет ошибки)

```dart
bool _isLoading = false;
```
**Объяснение:**
- `bool` - булево значение (true/false)
- `_isLoading` - флаг загрузки (показываем ли индикатор)

```dart
void _login() async {
```
**Объяснение:**
- `void` - функция ничего не возвращает
- `async` - асинхронная функция

```dart
if (!mounted) return;
```
**Объяснение:**
- `mounted` - свойство State, проверяет, не удален ли виджет из дерева
- `!mounted` - если виджет удален (`!` - отрицание)
- `return` - выходим из функции (безопасность от ошибок)

```dart
setState(() {
  _isLoading = true;
  _errorMessage = null;
});
```
**Объяснение:**
- `setState()` - метод для обновления состояния
- Внутри изменяем поля
- После `setState()` виджет перерисовывается (`build()` вызывается снова)

```dart
final email = _emailController.text.trim();
```
**Объяснение:**
- `_emailController.text` - получаем текст из поля ввода
- `.trim()` - убираем пробелы в начале и конце

```dart
final userData = await _authService.loginUser(email, password);
```
**Объяснение:**
- `await` - ждем завершения асинхронной операции
- Результат сохраняем в `userData`

```dart
if (userData != null) {
```
**Объяснение:**
- Проверяем, что данные получены (не null)
- `!=` - оператор "не равно"

```dart
await StorageService.saveUserData(
  userData['id'].toString(),
  userData['email'].toString(),
);
```
**Объяснение:**
- Сохраняем данные пользователя локально
- `userData['id']` - получаем значение по ключу
- `.toString()` - преобразуем в строку

```dart
Navigator.pushReplacement(
  context,
  MaterialPageRoute(
    builder: (_) => HomeScreen(
      userId: userData['id'].toString(),
      userEmail: userData['email'].toString(),
    ),
  ),
);
```
**Объяснение:**
- `Navigator` - навигация между экранами
- `pushReplacement()` - заменяет текущий экран новым (нельзя вернуться назад)
- `context` - контекст виджета (нужен для навигации)
- `MaterialPageRoute` - маршрут с Material Design анимацией
- `builder: (_) =>` - функция, которая создает экран
- `_` - игнорируем параметр (не используем)

```dart
@override
Widget build(BuildContext context) {
```
**Объяснение:** Метод, который строит UI. Вызывается при каждом `setState()`.

```dart
return Scaffold(
  appBar: AppBar(title: Text('Вход')),
```
**Объяснение:**
- `Scaffold` - базовый виджет экрана
- `appBar` - верхняя панель (AppBar)
- `AppBar(title: Text('Вход'))` - создаем AppBar с заголовком

```dart
body: Padding(
  padding: const EdgeInsets.all(16),
```
**Объяснение:**
- `body` - основное содержимое экрана
- `Padding` - добавляет отступы со всех сторон
- `EdgeInsets.all(16)` - отступы 16 пикселей со всех сторон
- `const` - константа времени компиляции

```dart
child: Column(
  children: [
```
**Объяснение:**
- `Column` - вертикальный список виджетов
- `children` - список дочерних виджетов
- `[]` - пустой список (будем добавлять виджеты)

```dart
TextField(
  controller: _emailController,
  decoration: InputDecoration(labelText: 'Email'),
),
```
**Объяснение:**
- `TextField` - поле ввода текста
- `controller` - контроллер для управления полем
- `decoration` - декорация (стиль, подсказка)
- `InputDecoration(labelText: 'Email')` - подсказка над полем

```dart
obscureText: true,
```
**Объяснение:** Скрывает введенный текст (для пароля).

```dart
if (_errorMessage != null)
  Text(_errorMessage!, style: TextStyle(color: Colors.red)),
```
**Объяснение:**
- `if` - условный оператор в списке виджетов
- Если условие true, виджет добавляется в список
- `_errorMessage!` - оператор `!` говорит "не null" (проверили выше)
- `TextStyle(color: Colors.red)` - красный цвет текста

```dart
if (_isLoading)
  CircularProgressIndicator()
else
  ElevatedButton(onPressed: _login, child: Text('Войти')),
```
**Объяснение:**
- Тернарный оператор: если `_isLoading` true, показываем индикатор, иначе кнопку
- `ElevatedButton` - кнопка с тенью
- `onPressed: _login` - функция, вызываемая при нажатии
- `child: Text('Войти')` - текст на кнопке

```dart
TextButton(
  onPressed: () {
    Navigator.push(
      context,
      MaterialPageRoute(builder: (_) => RegisterScreen()),
    );
  },
  child: Text('Нет аккаунта? Зарегистрироваться'),
),
```
**Объяснение:**
- `TextButton` - текстовая кнопка (без фона)
- `onPressed: () { }` - анонимная функция (лямбда)
- `Navigator.push()` - переходим на новый экран (можно вернуться назад)
- `RegisterScreen()` - создаем экран регистрации

---

## Паттерны и лучшие практики {#паттерны}

### 1. Разделение ответственности

**Плохо:**
```dart
// Вся логика в одном месте
class LoginScreen extends StatefulWidget {
  // API запросы, UI, валидация - все вместе
}
```

**Хорошо:**
```dart
// Разделяем на сервисы и экраны
class AuthService {
  // Только работа с API
}

class LoginScreen extends StatefulWidget {
  // Только UI и вызовы сервисов
}
```

### 2. Обработка ошибок

```dart
try {
  // Код, который может вызвать ошибку
  final data = await fetchData();
} catch (e) {
  // Обработка ошибки
  print('Ошибка: $e');
  // Показываем пользователю
  showError('Не удалось загрузить данные');
}
```

### 3. Проверка mounted

```dart
// ВАЖНО! Всегда проверяйте mounted перед setState
if (!mounted) return;
setState(() {
  // Изменения состояния
});
```

**Почему?** Если виджет удален из дерева, `setState()` вызовет ошибку.

### 4. Использование const

```dart
// Хорошо - const виджеты не пересоздаются
const Text('Привет')

// Плохо - создается новый объект каждый раз
Text('Привет')
```

### 5. Именование

```dart
// Классы - с большой буквы
class UserService {}

// Переменные и методы - camelCase
String userName = "Иван";
void getUserData() {}

// Приватные - с подчеркивания
final _privateField = "секрет";
void _privateMethod() {}

// Константы - UPPER_SNAKE_CASE
const MAX_USERS = 100;
```

---

## Работа с API и WebSocket {#api-websocket}

### HTTP запросы

```dart
// GET запрос
final response = await http.get(Uri.parse('$baseUrl/users'));

// POST запрос
final response = await http.post(
  Uri.parse('$baseUrl/users'),
  headers: {'Content-Type': 'application/json'},
  body: jsonEncode({'name': 'Иван'}),
);

// DELETE запрос
final response = await http.delete(
  Uri.parse('$baseUrl/users/123'),
);
```

### WebSocket

```dart
// Подключение
final channel = WebSocketChannel.connect(
  Uri.parse('wss://server.com?userId=123'),
);

// Прослушивание сообщений
channel.stream.listen((event) {
  final data = jsonDecode(event);
  // Обработка данных
});

// Отправка сообщения
channel.sink.add(jsonEncode({'message': 'Привет'}));

// Закрытие соединения
channel.sink.close();
```

---

## Часто задаваемые вопросы

### Q: Что такое `BuildContext`?
**A:** Контекст виджета в дереве. Нужен для:
- Навигации (`Navigator.push(context, ...)`)
- Получения тем (`Theme.of(context)`)
- Показывания диалогов (`showDialog(context: context, ...)`)

### Q: Когда использовать `StatefulWidget` vs `StatelessWidget`?
**A:**
- `StatelessWidget` - когда виджет не меняется (статический контент)
- `StatefulWidget` - когда нужно изменять состояние (формы, счетчики, списки)

### Q: Что такое `Future` и `async/await`?
**A:**
- `Future` - обещание результата в будущем (асинхронная операция)
- `async` - функция асинхронная
- `await` - ждем завершения Future

```dart
Future<String> getData() async {
  await Future.delayed(Duration(seconds: 1));
  return "Данные";
}
```

### Q: Что означает `?` после типа?
**A:** Тип может быть `null` (nullable).

```dart
String? name;  // Может быть null
String name;   // Не может быть null
```

### Q: Что такое `required`?
**A:** Обязательный параметр. Должен быть указан при вызове.

```dart
void greet({required String name}) {}
greet(name: "Иван");  // Обязательно указать name
```

---

## Полезные ресурсы

1. **Официальная документация Flutter:** https://flutter.dev/docs
2. **Dart Language Tour:** https://dart.dev/guides/language/language-tour
3. **Flutter Widget Catalog:** https://flutter.dev/docs/development/ui/widgets

---

## Заключение

Этот учебник покрывает основы проекта. Для глубокого понимания:
1. Читайте официальную документацию
2. Экспериментируйте с кодом
3. Изучайте примеры других проектов
4. Задавайте вопросы в сообществах Flutter

Удачи в изучении! 🚀

