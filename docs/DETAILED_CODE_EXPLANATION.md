# Детальный разбор кода - построчно
## Полное объяснение каждого файла проекта

---

## Оглавление

1. [services/storage_service.dart](#storage-service)
2. [models/chat.dart](#chat-model)
3. [models/user.dart](#user-model)
4. [screens/home_screen.dart](#home-screen)
5. [screens/chat_screen.dart](#chat-screen-detailed)
6. [services/chats_service.dart](#chats-service-detailed)
7. [services/messages_service.dart](#messages-service-detailed)
8. [Серверная часть (Node.js)](#server-side)

---

## services/storage_service.dart {#storage-service}

### Полный разбор построчно:

```dart
import 'package:shared_preferences/shared_preferences.dart';
```
**Объяснение:**
- `import` - импортируем пакет из pub.dev
- `package:` - указывает что это внешний пакет (не локальный файл)
- `shared_preferences` - название пакета
- `/shared_preferences.dart` - конкретный файл из пакета
- Этот пакет позволяет сохранять данные локально на устройстве (как localStorage в вебе)

```dart
class StorageService {
```
**Объяснение:**
- Определяем класс `StorageService`
- Класс - это шаблон для создания объектов
- Название с большой буквы (соглашение Dart)

```dart
static const String _userIdKey = 'user_id';
```
**Объяснение:**
- `static` - принадлежит классу, не экземпляру
  - Можно вызвать: `StorageService._userIdKey` (но приватное)
  - Не нужно создавать объект: `StorageService()`
- `const` - константа времени компиляции
  - Значение известно при компиляции
  - Нельзя изменить
- `String` - тип данных (строка)
- `_userIdKey` - приватное поле (начинается с `_`)
  - Недоступно извне класса
- `'user_id'` - значение константы (ключ для хранения)

**Зачем константы вместо строк напрямую?**
```dart
// Плохо (может быть опечатка):
prefs.setString('user_id', userId);  // Опечатка: 'user_id' vs 'user_Id'

// Хорошо (компилятор поймает ошибку):
prefs.setString(_userIdKey, userId);  // Если опечатка - ошибка компиляции
```

```dart
static Future<void> saveUserData(String userId, String userEmail) async {
```
**Объяснение:**
- `static` - метод класса (вызываем без создания объекта)
  - `StorageService.saveUserData(...)` вместо `StorageService().saveUserData(...)`
- `Future<void>` - асинхронная операция без возвращаемого значения
  - `Future` - обещание результата в будущем
  - `void` - ничего не возвращаем
- `async` - функция асинхронная (может использовать `await`)
- `String userId` - параметр типа String
- `String userEmail` - второй параметр

**Пример использования:**
```dart
await StorageService.saveUserData('123', 'user@example.com');
```

```dart
final prefs = await SharedPreferences.getInstance();
```
**Объяснение:**
- `final` - переменная не изменится после присваивания
- `prefs` - переменная для хранения экземпляра SharedPreferences
- `await` - ждем завершения асинхронной операции
- `SharedPreferences.getInstance()` - получаем экземпляр (singleton паттерн)
  - Singleton - всегда один и тот же объект
  - Эффективно - не создаем новый каждый раз

```dart
await prefs.setString(_userIdKey, userId);
```
**Объяснение:**
- `await` - ждем завершения записи
- `prefs.setString()` - сохраняем строку
- `_userIdKey` - ключ (константа из класса)
- `userId` - значение (параметр функции)
- Сохраняется в локальное хранилище устройства

**Как это работает:**
- На Android: SharedPreferences (XML файл)
- На iOS: UserDefaults (plist файл)
- На Web: localStorage

```dart
static Future<Map<String, String>?> getUserData() async {
```
**Объяснение:**
- Возвращает `Map<String, String>?` - может быть null
- `Map` - словарь/объект (ключ-значение)
- `String, String` - ключ и значение оба строки
- `?` - может вернуть null (если данных нет)

```dart
final userId = prefs.getString(_userIdKey);
```
**Объяснение:**
- `getString()` - получаем строку по ключу
- Возвращает `String?` (может быть null если ключа нет)
- `final` - переменная не изменится

```dart
if (userId != null && userEmail != null) {
```
**Объяснение:**
- `!=` - оператор "не равно"
- `null` - отсутствие значения
- `&&` - логическое И (оба условия должны быть true)
- Если оба значения не null - выполняем код внутри

```dart
return {
  'id': userId,
  'email': userEmail,
};
```
**Объяснение:**
- `return` - возвращаем значение из функции
- `{ }` - литерал объекта Map
- `'id': userId` - пара ключ-значение
- Создаем Map с данными пользователя

```dart
return null;
```
**Объяснение:**
- Если данных нет - возвращаем null
- Вызывающий код должен проверить на null

```dart
static Future<void> clearUserData() async {
```
**Объяснение:**
- Метод для очистки данных (при выходе)
- `clear` - очистить

```dart
await prefs.remove(_userIdKey);
```
**Объяснение:**
- `remove()` - удаляет значение по ключу
- После этого `getString(_userIdKey)` вернет null

---

## models/chat.dart {#chat-model}

```dart
class Chat {
```
**Объяснение:** Класс для представления чата в приложении.

```dart
final String id;
final String name;
final bool isGroup;
```
**Объяснение:**
- `final` - поля нельзя изменить после создания объекта
- `String id` - уникальный идентификатор чата
- `String name` - название чата
- `bool isGroup` - флаг группового чата (true/false)

```dart
Chat({required this.id, required this.name, required this.isGroup});
```
**Объяснение:**
- Конструктор с именованными параметрами
- `required` - параметры обязательны
- `this.id` - присваиваем параметр полю `id`
- Короткая запись вместо:
```dart
Chat({required String id, required String name, required bool isGroup}) {
  this.id = id;
  this.name = name;
  this.isGroup = isGroup;
}
```

**Пример использования:**
```dart
final chat = Chat(
  id: '1',
  name: 'Мой чат',
  isGroup: false,
);
```

```dart
factory Chat.fromJson(Map<String, dynamic> json) {
```
**Объяснение:**
- `factory` - фабричный конструктор
  - Может возвращать существующий объект
  - Или создавать новый
  - Используется для создания из JSON
- `fromJson` - стандартное имя для создания из JSON
- `Map<String, dynamic>` - JSON объект (ключи - строки, значения - любые)

```dart
try {
```
**Объяснение:**
- Начало блока обработки ошибок
- Если что-то пойдет не так - выполнится `catch`

```dart
return Chat(
  id: (json['id'] ?? '').toString(),
```
**Объяснение:**
- `json['id']` - получаем значение по ключу 'id'
- `??` - если null, используем '' (пустую строку)
- `.toString()` - преобразуем в строку
- На случай если сервер вернул число вместо строки

```dart
name: json['name'] ?? '',
```
**Объяснение:**
- Получаем название чата
- Если null - пустая строка

```dart
isGroup: _parseBool(json['is_group']),
```
**Объяснение:**
- `json['is_group']` - получаем значение из JSON
- `_parseBool()` - вызываем приватный метод для безопасного парсинга
- Нужно потому что сервер может вернуть bool, int (0/1) или строку ('true'/'false')

```dart
} catch (e) {
  print('Error parsing Chat from JSON: $e');
  print('JSON: $json');
  rethrow;
}
```
**Объяснение:**
- `catch (e)` - ловим ошибку в переменную `e`
- `print()` - выводим в консоль для отладки
- `$e` - интерполяция (вставляем значение переменной)
- `rethrow` - пробрасываем ошибку дальше
  - Не скрываем ошибку
  - Позволяем обработать выше по стеку

```dart
static bool _parseBool(dynamic value) {
```
**Объяснение:**
- `static` - метод класса (не нужен экземпляр)
- `bool` - возвращаемый тип
- `dynamic` - параметр может быть любого типа
- Приватный метод (начинается с `_`)

```dart
if (value is bool) return value;
```
**Объяснение:**
- `is` - проверка типа
- Если уже bool - возвращаем как есть
- `return` - выходим из функции

```dart
if (value is int) return value == 1;
```
**Объяснение:**
- Если число - проверяем равенство 1
- `==` - оператор сравнения
- `1` = true, `0` = false (как в SQL/базах данных)

```dart
if (value is String) {
  return value.toLowerCase() == 'true' || value == '1';
}
```
**Объяснение:**
- Если строка - проверяем содержимое
- `.toLowerCase()` - приводим к нижнему регистру
  - 'True', 'TRUE', 'true' - все станут 'true'
- `== 'true'` - проверка на строку 'true'
- `||` - логическое ИЛИ
- `== '1'` - проверка на строку '1'
- Если хотя бы одно условие true - возвращаем true

```dart
return false;
```
**Объяснение:**
- Если тип не распознан - возвращаем false по умолчанию
- Безопасное значение

---

## models/user.dart {#user-model}

```dart
class User {
  final String id;
  final String email;
```
**Объяснение:**
- Простая модель пользователя
- Только ID и email (минимальная информация)

```dart
User({required this.id, required this.email});
```
**Объяснение:**
- Конструктор с обязательными параметрами
- Оба параметра обязательны

```dart
factory User.fromJson(Map<String, dynamic> json) {
  return User(
    id: json['id'].toString(),
    email: json['email'],
  );
}
```
**Объяснение:**
- Фабричный конструктор из JSON
- `json['id'].toString()` - преобразуем ID в строку
- `json['email']` - email уже строка (не нужно преобразование)
- Простая версия без обработки ошибок (можно добавить try-catch)

---

## screens/home_screen.dart {#home-screen}

### Импорты:

```dart
import 'package:flutter/material.dart';
```
**Объяснение:** Основной пакет Flutter для Material Design.

```dart
import '../models/chat.dart';
```
**Объяснение:**
- `../` - поднимаемся на уровень выше (из screens в lib)
- `models/chat.dart` - импортируем модель Chat

```dart
import '../services/chats_service.dart';
```
**Объяснение:** Импортируем сервис для работы с чатами.

### Класс HomeScreen:

```dart
class HomeScreen extends StatefulWidget {
```
**Объяснение:**
- `StatefulWidget` - виджет с состоянием
- Может изменяться и перерисовываться

```dart
final String userId;
final String userEmail;
```
**Объяснение:**
- Параметры виджета
- `final` - нельзя изменить после создания
- Передаются при создании виджета

```dart
HomeScreen({required this.userId, required this.userEmail});
```
**Объяснение:**
- Конструктор с именованными обязательными параметрами
- При создании нужно указать оба параметра

```dart
@override
_HomeScreenState createState() => _HomeScreenState();
```
**Объяснение:**
- `@override` - переопределяем метод родителя
- `createState()` - создает объект состояния
- `=>` - стрелочная функция (короткая запись)
- `_HomeScreenState` - класс состояния (приватный)

### Класс _HomeScreenState:

```dart
class _HomeScreenState extends State<HomeScreen> {
```
**Объяснение:**
- `extends State<HomeScreen>` - наследуемся от State
- `<HomeScreen>` - указываем тип виджета
- Приватный класс (начинается с `_`)

```dart
final ChatsService _chatsService = ChatsService();
```
**Объяснение:**
- Создаем экземпляр сервиса
- `final` - не изменится
- Приватное поле (начинается с `_`)

```dart
List<Chat> _chats = [];
```
**Объяснение:**
- Список чатов
- Изначально пустой
- Будет заполняться при загрузке

```dart
bool _isLoading = false;
```
**Объяснение:**
- Флаг загрузки
- `false` - не загружается
- Используется для показа индикатора загрузки

```dart
@override
void initState() {
  super.initState();
```
**Объяснение:**
- `initState()` - вызывается один раз при создании виджета
- `super.initState()` - вызываем метод родителя (обязательно!)
- Идеальное место для инициализации

```dart
print('HomeScreen initialized with userId: ${widget.userId}');
```
**Объяснение:**
- `widget` - доступ к виджету из State
- `widget.userId` - получаем параметр виджета
- `${}` - интерполяция в строке
- `print()` - выводим в консоль для отладки

```dart
_loadChats();
```
**Объяснение:**
- Вызываем метод загрузки чатов
- Выполнится сразу после инициализации

```dart
Future<void> _loadChats() async {
```
**Объяснение:**
- Асинхронный метод загрузки чатов
- `Future<void>` - ничего не возвращает
- `async` - может использовать await

```dart
if (!mounted) return;
```
**Объяснение:**
- Проверка, не удален ли виджет
- `mounted` - свойство State
- `!mounted` - если виджет удален
- `return` - выходим из функции (безопасность)

```dart
setState(() => _isLoading = true);
```
**Объяснение:**
- Обновляем состояние
- Стрелочная функция для короткого кода
- Устанавливаем флаг загрузки в true
- Виджет перерисуется

```dart
try {
```
**Объяснение:**
- Начало блока обработки ошибок
- Если произойдет ошибка - выполнится catch

```dart
final chats = await _chatsService.fetchChats(widget.userId);
```
**Объяснение:**
- Вызываем метод сервиса
- `await` - ждем результата
- Результат сохраняем в `chats`

```dart
print('Loaded ${chats.length} chats');
```
**Объяснение:**
- Выводим количество загруженных чатов
- `${chats.length}` - интерполяция с выражением
- `.length` - свойство списка (количество элементов)

```dart
if (mounted) {
  setState(() {
    _chats = chats;
  });
}
```
**Объяснение:**
- Проверяем mounted перед setState
- Обновляем список чатов
- Виджет перерисуется с новыми данными

```dart
} catch (e) {
```
**Объяснение:**
- Ловим любые ошибки
- `e` - переменная с ошибкой

```dart
ScaffoldMessenger.of(context).showSnackBar(
```
**Объяснение:**
- `ScaffoldMessenger` - показывает уведомления
- `.of(context)` - получаем из контекста
- `showSnackBar()` - показываем уведомление

```dart
SnackBar(content: Text('Ошибка при загрузке чатов: $e')),
```
**Объяснение:**
- `SnackBar` - виджет уведомления
- Появляется снизу экрана
- `content` - содержимое уведомления

```dart
} finally {
```
**Объяснение:**
- Выполняется всегда (даже если была ошибка)
- Идеально для очистки (убрать индикатор загрузки)

```dart
if (mounted) {
  setState(() => _isLoading = false);
}
```
**Объяснение:**
- Убираем индикатор загрузки
- Выполнится в любом случае (успех или ошибка)

```dart
void _openChat(Chat chat) {
```
**Объяснение:**
- Метод для открытия чата
- Принимает объект Chat

```dart
Navigator.push(
```
**Объяснение:**
- Переходим на новый экран
- Можно вернуться назад (в отличие от pushReplacement)

```dart
context,
```
**Объяснение:**
- Контекст виджета
- Нужен для навигации

```dart
MaterialPageRoute(
```
**Объяснение:**
- Маршрут с Material Design анимацией
- Плавный переход между экранами

```dart
builder: (_) => ChatScreen(
```
**Объяснение:**
- `builder` - функция, создающая экран
- `_` - игнорируем параметр
- `=>` - стрелочная функция
- Создаем экран ChatScreen

```dart
userId: widget.userId,
userEmail: widget.userEmail,
chatId: chat.id,
chatName: chat.name,
```
**Объяснение:**
- Передаем параметры в ChatScreen
- `widget.userId` - из текущего виджета
- `chat.id` - из объекта чата

```dart
Future<void> _deleteChat(Chat chat) async {
```
**Объяснение:**
- Асинхронный метод удаления чата
- Принимает объект Chat

```dart
final confirmed = await showDialog<bool>(
```
**Объяснение:**
- Показываем диалог подтверждения
- `await` - ждем закрытия диалога
- `<bool>` - тип возвращаемого значения
- Результат сохраняем в `confirmed`

```dart
context: context,
```
**Объяснение:**
- Контекст для диалога
- Нужен для показа диалога

```dart
builder: (context) => AlertDialog(
```
**Объяснение:**
- Функция, создающая диалог
- `AlertDialog` - стандартный диалог Material Design

```dart
title: Text('Удалить чат?'),
```
**Объяснение:**
- Заголовок диалога
- `Text` - виджет текста

```dart
content: Text('Вы уверены, что хотите удалить чат "${chat.name}"? Это действие нельзя отменить.'),
```
**Объяснение:**
- Содержимое диалога
- `"${chat.name}"` - интерполяция с выражением
- Вставляем название чата в текст

```dart
actions: [
```
**Объяснение:**
- Список кнопок действий
- `[ ]` - массив виджетов

```dart
TextButton(
  onPressed: () => Navigator.pop(context, false),
  child: Text('Отмена'),
),
```
**Объяснение:**
- `TextButton` - текстовая кнопка
- `onPressed` - функция при нажатии
- `Navigator.pop()` - закрываем диалог
- `false` - возвращаемое значение (отмена)

```dart
ElevatedButton(
  onPressed: () => Navigator.pop(context, true),
```
**Объяснение:**
- `ElevatedButton` - кнопка с тенью
- `true` - возвращаемое значение (подтверждение)

```dart
style: ElevatedButton.styleFrom(
  backgroundColor: Colors.red,
),
```
**Объяснение:**
- `styleFrom()` - создаем стиль
- `backgroundColor` - цвет фона
- Красный для опасного действия

```dart
if (confirmed != true || !mounted) return;
```
**Объяснение:**
- Проверяем подтверждение
- `!=` - не равно
- `||` - логическое ИЛИ
- Если не подтверждено или виджет удален - выходим

```dart
await _chatsService.deleteChat(chat.id, widget.userId);
```
**Объяснение:**
- Вызываем метод удаления
- `await` - ждем завершения

```dart
ScaffoldMessenger.of(context).showSnackBar(
  SnackBar(
    content: Text('Чат "${chat.name}" удален'),
    duration: const Duration(seconds: 2),
  ),
);
```
**Объяснение:**
- Показываем уведомление об успехе
- `duration` - длительность показа
- `const Duration()` - константа времени компиляции

```dart
_loadChats();
```
**Объяснение:**
- Обновляем список чатов
- После удаления нужно перезагрузить

```dart
void _logout() {
```
**Объяснение:**
- Метод выхода из аккаунта
- Не асинхронный (но внутри есть await)

```dart
showDialog(
```
**Объяснение:**
- Показываем диалог подтверждения
- Без await (не ждем результата здесь)

```dart
ElevatedButton(
  onPressed: () async {
```
**Объяснение:**
- Анонимная асинхронная функция
- `async` - может использовать await

```dart
Navigator.pop(context);
```
**Объяснение:**
- Закрываем диалог
- Выполняется сразу при нажатии

```dart
await StorageService.clearUserData();
```
**Объяснение:**
- Очищаем сохраненные данные
- `await` - ждем завершения

```dart
Navigator.pushAndRemoveUntil(
```
**Объяснение:**
- Переходим на экран и удаляем все предыдущие
- Нельзя вернуться назад

```dart
MaterialPageRoute(builder: (_) => LoginScreen()),
```
**Объяснение:**
- Создаем маршрут к LoginScreen

```dart
(route) => false,
```
**Объяснение:**
- Функция-предикат для удаления маршрутов
- `false` - удаляем все маршруты
- Можно указать условие: `(route) => route.isFirst` - удалить все кроме первого

---

## screens/chat_screen.dart - Детальный разбор {#chat-screen-detailed}

### Импорты:

```dart
import 'dart:async';
```
**Объяснение:**
- Импортируем библиотеку для работы с асинхронностью
- Нужна для `StreamSubscription`

```dart
import 'dart:convert';
```
**Объяснение:**
- Импортируем библиотеку для работы с JSON
- `jsonDecode()`, `jsonEncode()`

```dart
import 'package:web_socket_channel/web_socket_channel.dart';
```
**Объяснение:**
- Пакет для WebSocket соединений
- Real-time коммуникация с сервером

```dart
import 'package:intl/intl.dart';
```
**Объяснение:**
- Пакет для интернационализации и форматирования
- Используем для форматирования дат

### Поля класса:

```dart
final _controller = TextEditingController();
```
**Объяснение:**
- Контроллер для поля ввода текста
- Позволяет читать/писать текст
- Слушать изменения

```dart
late WebSocketChannel _channel;
```
**Объяснение:**
- `late` - будет инициализировано позже
- Компилятор проверяет инициализацию перед использованием
- Используем когда не можем инициализировать в объявлении

```dart
StreamSubscription? _webSocketSubscription;
```
**Объяснение:**
- Подписка на поток WebSocket
- `?` - может быть null
- Нужна для отмены подписки

```dart
List<Message> _messages = [];
```
**Объяснение:**
- Список сообщений
- Изначально пустой
- Тип элементов - `Message`

### initState:

```dart
@override
void initState() {
  super.initState();
```
**Объяснение:**
- Вызывается один раз при создании
- `super.initState()` - обязательно!

```dart
_channel = WebSocketChannel.connect(
  Uri.parse('wss://my-server-chat.onrender.com?userId=${widget.userId}'),
);
```
**Объяснение:**
- Подключаемся к WebSocket серверу
- `wss://` - безопасное соединение (как https)
- `?userId=...` - query параметр (передаем ID пользователя)
- `${widget.userId}` - интерполяция

```dart
_webSocketSubscription = _channel.stream.listen(
```
**Объяснение:**
- Подписываемся на поток данных
- `stream` - поток событий от сервера
- `listen()` - слушаем события
- Возвращает подписку для управления

```dart
(event) {
```
**Объяснение:**
- Обработчик каждого события
- `event` - данные от сервера (строка JSON)

```dart
if (!mounted) return;
```
**Объяснение:**
- Проверка перед обработкой
- Если виджет удален - выходим

```dart
final data = jsonDecode(event);
```
**Объяснение:**
- Парсим JSON строку в Map
- `event` - строка JSON
- `data` - объект Map

```dart
final messageType = data['type'];
```
**Объяснение:**
- Получаем тип сообщения
- Может быть `'message_deleted'` или обычное сообщение

```dart
if (messageType == 'message_deleted') {
```
**Объяснение:**
- Проверяем тип
- Если уведомление об удалении - обрабатываем отдельно

```dart
final deletedMessageId = data['message_id']?.toString();
```
**Объяснение:**
- `?.` - безопасный доступ (если null - вернет null)
- `.toString()` - преобразуем в строку

```dart
_messages.removeWhere((m) => m.id.toString() == deletedMessageId);
```
**Объяснение:**
- `removeWhere()` - удаляет элементы по условию
- `(m) =>` - функция для каждого элемента
- `m.id.toString() == deletedMessageId` - условие удаления
- Удаляем сообщение с таким ID

```dart
return;
```
**Объяснение:**
- Выходим из обработчика
- Не обрабатываем дальше

```dart
final chatId = data['chat_id']?.toString() ?? data['chatId']?.toString();
```
**Объяснение:**
- Получаем ID чата
- `??` - если первый null, используем второй
- Поддержка разных форматов данных

```dart
if (chatId == currentChatId) {
```
**Объяснение:**
- Проверяем, для нашего ли чата сообщение
- Обрабатываем только сообщения текущего чата

```dart
final message = Message.fromJson(data);
```
**Объяснение:**
- Создаем объект Message из JSON
- `fromJson()` - фабричный конструктор

```dart
final exists = _messages.any((m) => m.id == message.id);
```
**Объяснение:**
- Проверяем, есть ли уже такое сообщение
- `any()` - есть ли хотя бы один элемент
- Предотвращает дубликаты

```dart
if (!exists) {
  _messages.add(message);
```
**Объяснение:**
- Если нет - добавляем
- `!exists` - отрицание

```dart
_messages.sort((a, b) {
```
**Объяснение:**
- Сортируем список
- `(a, b)` - функция сравнения

```dart
final aTime = DateTime.parse(a.createdAt);
final bTime = DateTime.parse(b.createdAt);
return aTime.compareTo(bTime);
```
**Объяснение:**
- Парсим даты
- `compareTo()` - сравнивает даты
- Возвращает отрицательное, 0 или положительное

```dart
onError: (error) {
```
**Объяснение:**
- Обработчик ошибок WebSocket
- Вызывается при ошибке соединения

```dart
onDone: () {
```
**Объяснение:**
- Обработчик закрытия соединения
- Вызывается когда сервер закрыл соединение

### Метод _formatDate:

```dart
String _formatDate(String dateString) {
```
**Объяснение:**
- Форматирует дату в читаемый вид
- Принимает строку, возвращает строку

```dart
final date = DateTime.parse(dateString);
```
**Объяснение:**
- Парсим строку в объект DateTime
- Формат: ISO 8601 (например: "2024-12-09T15:30:00Z")

```dart
final now = DateTime.now();
```
**Объяснение:**
- Текущая дата и время
- Используем для сравнения

```dart
final difference = now.difference(date);
```
**Объяснение:**
- Вычисляем разницу
- Возвращает `Duration` (продолжительность)

```dart
if (difference.inDays == 0) {
```
**Объяснение:**
- Если разница 0 дней - сегодня
- `inDays` - свойство Duration (количество дней)

```dart
return DateFormat('HH:mm').format(date);
```
**Объяснение:**
- Форматируем только время
- `'HH:mm'` - формат (24-часовой, часы:минуты)
- Пример: "15:30"

```dart
else if (difference.inDays == 1) {
  return 'Вчера ${DateFormat('HH:mm').format(date)}';
}
```
**Объяснение:**
- Если вчера - показываем "Вчера" + время
- Интерполяция строки

```dart
final weekdays = ['Пн', 'Вт', 'Ср', 'Чт', 'Пт', 'Сб', 'Вс'];
```
**Объяснение:**
- Массив названий дней недели
- Индекс 0 = Понедельник

```dart
final weekday = weekdays[date.weekday - 1];
```
**Объяснение:**
- `date.weekday` - день недели (1-7, где 1 = Понедельник)
- `- 1` - для индексации массива (0-6)

```dart
return DateFormat('dd.MM.yyyy HH:mm').format(date);
```
**Объяснение:**
- Полная дата для старых сообщений
- `'dd.MM.yyyy HH:mm'` - формат
- Пример: "09.12.2024 15:30"

### Метод _sendMessage:

```dart
Future<void> _sendMessage() async {
```
**Объяснение:**
- Асинхронный метод отправки сообщения

```dart
final text = _controller.text.trim();
```
**Объяснение:**
- Получаем текст из поля ввода
- `.trim()` - убираем пробелы

```dart
if (text.isEmpty || !mounted) return;
```
**Объяснение:**
- Проверяем на пустоту и состояние виджета
- Если пусто или удален - выходим

```dart
await _messagesService.sendMessage(widget.userId, widget.chatId, text);
```
**Объяснение:**
- Отправляем сообщение через сервис
- `await` - ждем завершения

```dart
_controller.clear();
```
**Объяснение:**
- Очищаем поле ввода
- После успешной отправки

### Метод dispose:

```dart
@override
void dispose() {
```
**Объяснение:**
- Вызывается при удалении виджета
- Идеальное место для очистки ресурсов

```dart
_webSocketSubscription?.cancel();
```
**Объяснение:**
- Отменяем подписку на поток
- `?.` - безопасный вызов (если null - ничего не произойдет)
- Важно! Иначе утечка памяти

```dart
_channel.sink.close();
```
**Объяснение:**
- Закрываем WebSocket соединение
- `sink` - канал для отправки данных
- Освобождаем ресурсы

```dart
_controller.dispose();
```
**Объяснение:**
- Освобождаем ресурсы контроллера
- Обязательно! Иначе утечка памяти

```dart
super.dispose();
```
**Объяснение:**
- Вызываем метод родителя
- Обязательно в конце!

---

## services/chats_service.dart - Детальный разбор {#chats-service-detailed}

```dart
Future<List<Chat>> fetchChats(String userId) async {
```
**Объяснение:**
- Возвращает список чатов
- `List<Chat>` - список объектов Chat
- `Future` - асинхронная операция

```dart
final response = await http.get(Uri.parse('$baseUrl/chats/$userId'));
```
**Объяснение:**
- GET запрос к серверу
- `$baseUrl/chats/$userId` - интерполяция в строке
- Пример URL: `https://server.com/chats/123`

```dart
final List<dynamic> data = jsonDecode(response.body);
```
**Объяснение:**
- Парсим JSON ответ
- `List<dynamic>` - список любых типов
- `dynamic` - потому что еще не знаем структуру

```dart
final List<Chat> chats = [];
```
**Объяснение:**
- Создаем пустой список для объектов Chat
- Будем заполнять в цикле

```dart
for (var chatJson in data) {
```
**Объяснение:**
- Цикл по элементам списка
- `for...in` - перебираем каждый элемент
- `var` - тип выводится автоматически
- `chatJson` - текущий элемент (JSON объект)

```dart
chats.add(Chat.fromJson(chatJson as Map<String, dynamic>));
```
**Объяснение:**
- Преобразуем JSON в объект Chat
- `as` - приведение типа
- `fromJson()` - фабричный конструктор
- Добавляем в список

```dart
rethrow;
```
**Объяснение:**
- Пробрасываем ошибку дальше
- Не скрываем, позволяем обработать выше

```dart
Future<Chat> createChat(String name, List<String> userIds) async {
```
**Объяснение:**
- Создает чат и возвращает его
- `List<String>` - список ID пользователей

```dart
body: jsonEncode({
  'name': name,
  'userIds': userIds,
}),
```
**Объяснение:**
- Преобразуем Map в JSON строку
- Отправляем в теле запроса

```dart
).timeout(
  const Duration(seconds: 10),
  onTimeout: () {
    throw Exception('Таймаут при создании чата');
  },
);
```
**Объяснение:**
- Устанавливаем таймаут
- Если запрос длится больше 10 секунд - ошибка
- `onTimeout` - функция при таймауте

```dart
if (responseData is! Map<String, dynamic>) {
```
**Объяснение:**
- Проверяем тип ответа
- `is!` - не является
- Если не Map - ошибка формата

---

## Серверная часть (Node.js/Express) {#server-side}

### controllers/messagesController.js

```javascript
import pool from '../db.js';
```
**Объяснение:**
- ES6 синтаксис импорта
- `pool` - пул соединений с БД
- Пул эффективнее чем создавать соединение каждый раз

```javascript
export const getMessages = async (req, res) => {
```
**Объяснение:**
- `export` - экспортируем функцию
- `async` - асинхронная функция
- `req` - объект запроса
- `res` - объект ответа

```javascript
const chatId = req.params.chatId;
```
**Объяснение:**
- Получаем параметр из URL
- Если URL: `/messages/123`, то `req.params.chatId = '123'`

```javascript
const result = await pool.query(`
  SELECT ...
  FROM messages
  JOIN users ON messages.user_id = users.id
  WHERE messages.chat_id = $1
`, [chatId]);
```
**Объяснение:**
- SQL запрос с параметризацией
- `$1` - первый параметр (безопасно от SQL инъекций)
- `[chatId]` - значения параметров

```javascript
const formattedMessages = result.rows.map(row => ({
```
**Объяснение:**
- Преобразуем каждую строку результата
- `.map()` - создает новый массив
- `row =>` - функция для каждого элемента
- `({ })` - возвращаем объект

```javascript
res.json(formattedMessages);
```
**Объяснение:**
- Отправляем JSON ответ
- Автоматически устанавливает заголовок Content-Type

---

## Продолжение следует...

Этот документ содержит детальный разбор основных файлов. Для полного понимания рекомендую:

1. Читать код вместе с этим учебником
2. Экспериментировать - изменять код и смотреть что происходит
3. Использовать отладчик (debugger) для пошагового выполнения
4. Читать официальную документацию Flutter и Dart

Удачи в изучении! 🚀

