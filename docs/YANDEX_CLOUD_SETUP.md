# 🚀 Настройка Яндекс Object Storage

## Инструкция по настройке хранения изображений в Яндекс Облаке

---

## Шаг 1: Регистрация в Яндекс Облаке

1. Перейдите на https://cloud.yandex.ru/
2. Нажмите **"Создать аккаунт"** или **"Войти"**
3. Заполните данные и подтвердите email/телефон

---

## Шаг 2: Создание каталога

1. После входа в консоль, создайте **каталог** (если еще не создан)
2. Выберите каталог для работы

---

## Шаг 3: Создание бакета Object Storage

1. В меню слева выберите **Object Storage**
2. Нажмите **"Создать бакет"**
3. Заполните параметры:
   - **Имя бакета**: `my-chat-images` (или другое уникальное имя)
   - **Тип доступа**: **Публичный** (чтобы изображения были доступны по URL)
   - **Регион**: `ru-central1` (Москва)
   - **Класс хранилища**: **Стандартное**
4. Нажмите **"Создать"**

---

## Шаг 4: Создание сервисного аккаунта

1. В меню слева выберите **IAM** → **Сервисные аккаунты**
2. Нажмите **"Создать сервисный аккаунт"**
3. Заполните:
   - **Имя**: `chat-storage-service`
   - **Описание**: `Сервисный аккаунт для загрузки изображений`
4. Нажмите **"Создать"**

---

## Шаг 5: Назначение роли сервисному аккаунту

1. В списке сервисных аккаунтов найдите созданный аккаунт
2. Нажмите на него
3. Перейдите на вкладку **"Роли"**
4. Нажмите **"Назначить роли"**
5. Выберите:
   - **Каталог**: ваш каталог
   - **Роль**: `storage.editor` (для записи и чтения)
6. Нажмите **"Сохранить"**

---

## Шаг 6: Создание статического ключа доступа

1. В карточке сервисного аккаунта перейдите на вкладку **"Ключи"**
2. Нажмите **"Создать ключ"** → **"Статический ключ доступа"**
3. Заполните:
   - **Описание**: `Ключ для Object Storage`
4. Нажмите **"Создать"**
5. **ВАЖНО**: Сохраните оба значения:
   - **Access Key ID** (идентификатор ключа)
   - **Secret Access Key** (секретный ключ)

⚠️ **Секретный ключ показывается только один раз!** Сохраните его в безопасном месте.

---

## Шаг 7: Настройка CORS (опционально, но рекомендуется)

1. В Object Storage выберите ваш бакет
2. Перейдите на вкладку **"CORS"**
3. Нажмите **"Настроить CORS"**
4. Добавьте правило:
   - **Разрешенные источники**: `*` (или конкретные домены)
   - **Разрешенные методы**: `GET, HEAD, OPTIONS`
   - **Разрешенные заголовки**: `*`
   - **Максимальный возраст**: `3600`
5. Нажмите **"Сохранить"**

---

## Шаг 8: Настройка переменных окружения на Render.com

1. Перейдите на https://render.com/
2. Выберите ваш сервис (backend)
3. Перейдите в **Environment** (Переменные окружения)
4. Добавьте следующие переменные:

```env
YANDEX_ACCESS_KEY_ID=ваш_access_key_id
YANDEX_SECRET_ACCESS_KEY=ваш_secret_access_key
YANDEX_BUCKET_NAME=my-chat-images
YANDEX_STORAGE_URL=https://my-chat-images.storage.yandexcloud.net
```

**Где взять значения:**
- `YANDEX_ACCESS_KEY_ID` - Access Key ID из шага 6
- `YANDEX_SECRET_ACCESS_KEY` - Secret Access Key из шага 6
- `YANDEX_BUCKET_NAME` - имя вашего бакета (из шага 3)
- `YANDEX_STORAGE_URL` - URL вашего бакета (замените `my-chat-images` на имя вашего бакета)

---

## Шаг 9: Установка зависимостей

На Render.com зависимости установятся автоматически при деплое, но если нужно установить локально:

```bash
cd my_serve_chat_test
npm install
```

Это установит `@aws-sdk/client-s3` для работы с Яндекс Object Storage.

---

## Шаг 10: Перезапуск сервера

1. На Render.com перейдите в ваш сервис
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Или просто подождите автоматического деплоя после push в Git

---

## Проверка работы

1. Запустите приложение
2. Попробуйте загрузить изображение в чат
3. Проверьте логи сервера - должны быть сообщения:
   - `Uploading to Yandex Cloud: ...`
   - `Image uploaded successfully: https://...`
4. Проверьте, что изображение отображается в чате
5. Проверьте в консоли Яндекс Облака - файл должен появиться в бакете

---

## Структура файлов в бакете

Файлы будут храниться по пути:
```
my-chat-images/
  └── images/
      ├── image-1234567890-987654321.jpg
      ├── image-1234567891-123456789.png
      └── ...
```

URL для доступа:
```
https://my-chat-images.storage.yandexcloud.net/images/image-xxx.jpg
```

---

## Стоимость

### Бесплатно каждый месяц:
- Первый 1 ГБ хранения
- Первые 10,000 операций PUT (загрузка)
- Первые 100,000 операций GET (просмотр)
- Первые 100 ГБ исходящего трафика

### Платно:
- Хранение: 2,17₽ за ГБ/месяц (после первого ГБ)
- Трафик: 1,65₽ за ГБ (после первых 100 ГБ)
- Операции: 0,5184₽ за 1,000 PUT (после первых 10,000)

**Для вашего проекта: ~8-20₽/месяц**

---

## Устранение проблем

### Ошибка: "Access Denied"
- Проверьте, что сервисный аккаунт имеет роль `storage.editor`
- Проверьте правильность Access Key ID и Secret Access Key

### Ошибка: "Bucket not found"
- Проверьте имя бакета в переменной `YANDEX_BUCKET_NAME`
- Убедитесь, что бакет существует в вашем каталоге

### Ошибка: "CORS policy"
- Настройте CORS в настройках бакета (шаг 7)
- Проверьте, что разрешены методы GET, HEAD, OPTIONS

### Изображения не отображаются
- Проверьте, что бакет имеет тип доступа **"Публичный"**
- Проверьте URL изображения в логах
- Откройте URL напрямую в браузере

---

## Дополнительная информация

- [Документация Яндекс Object Storage](https://cloud.yandex.ru/docs/storage/)
- [Тарифы Object Storage](https://cloud.yandex.ru/docs/storage/pricing)
- [S3-совместимое API](https://cloud.yandex.ru/docs/storage/s3/)

---

## Готово! 🎉

Теперь все изображения будут храниться в Яндекс Облаке и не будут удаляться при перезапуске сервера.

