# 🔍 Проблема с токенами на веб (Vercel)

## Проблема

На веб (Vercel) токены не сохраняются или не читаются, из-за чего возникает ошибка 401.

## 🔍 Диагностика

### 1. Проверьте консоль браузера (F12)

**После входа должны увидеть:**
```
✅ Login успешен, получены данные: ...
💾 Сохранение токена...
✅ Токен сохранен в SharedPreferences: eyJhbGciOiJIUzI1NiIs...
✅ Платформа: WEB - данные сохранены
```

**Если НЕ видите эти логи:**
→ Токен не сохраняется при входе

### 2. Проверьте localStorage вручную

**Откройте DevTools → Application → Local Storage:**

Должны увидеть ключи:
- `flutter.user_id` = "5"
- `flutter.user_email` = "vlad"
- `flutter.auth_token` = "eyJhbGciOiJIUzI1NiIs..."

**Если ключей нет:**
→ SharedPreferences не работает на веб

### 3. Проверьте автовход

**При загрузке страницы должны увидеть:**
```
🔍 getUserData вызван
✅ SharedPreferences получен
🔍 getUserData результаты:
   userId: 5
   userEmail: vlad
   token: eyJhbGciOiJIUzI1NiIs...
✅ Все данные найдены, возвращаем Map
✅ Автоматический вход: userId=5, email=vlad
```

**Если видите:**
```
⚠️ Не все данные найдены:
   token: НЕТ
```
→ Токен не сохраняется или не читается

### 4. Проверьте запросы

**При загрузке чатов должны увидеть:**
```
🔍 _getAuthHeaders вызван
🔍 getToken вернул: токен найден
✅ Токен получен из SharedPreferences: eyJhbGciOiJIUzI1NiIs...
📤 Отправка запроса с токеном: eyJhbGciOiJIUzI1NiIs...
📋 Итоговые заголовки: [Content-Type, Authorization]
```

**Если видите:**
```
❌ КРИТИЧНО: Запрос отправляется БЕЗ токена!
```
→ Токен не читается из хранилища

## 🐛 Возможные причины:

### 1. SharedPreferences не работает на веб

**Симптомы:**
- Токен не сохраняется
- localStorage пустой

**Решение:**
- Проверьте версию `shared_preferences` в `pubspec.yaml`
- Обновите до последней версии: `flutter pub upgrade shared_preferences`

### 2. Токен не сохраняется при входе

**Симптомы:**
- Нет логов "✅ Токен сохранен"
- localStorage пустой

**Решение:**
- Проверьте, что сервер возвращает токен в ответе
- Проверьте логи входа

### 3. Токен не читается из хранилища

**Симптомы:**
- localStorage содержит токен
- Но `getToken()` возвращает null

**Решение:**
- Может быть проблема с префиксом ключей на веб
- Проверьте ключ в localStorage - должен быть `flutter.auth_token`

## 🔧 Временное решение:

Если проблема критична, можно использовать localStorage напрямую через JavaScript interop, но это требует дополнительной настройки.

## 📝 Следующие шаги:

1. **Откройте консоль браузера** (F12)
2. **Войдите заново** - посмотрите логи сохранения
3. **Проверьте localStorage** - есть ли там токен?
4. **Обновите страницу** - посмотрите логи чтения
5. **Попробуйте загрузить чаты** - посмотрите логи запроса

---

**Детальное логирование добавлено! Проверьте консоль для диагностики.**

