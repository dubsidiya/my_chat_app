# Роуты и Контроллеры - Полное руководство
## Как работает серверная часть вашего приложения

---

## Оглавление

1. [Что такое Роуты?](#что-такое-роуты)
2. [Что такое Контроллеры?](#что-такое-контроллеры)
3. [Как они работают вместе](#как-они-работают-вместе)
4. [Детальный разбор на примерах](#детальный-разбор)
5. [HTTP методы](#http-методы)
6. [Параметры запросов](#параметры-запросов)
7. [Обработка ошибок](#обработка-ошибок)
8. [Лучшие практики](#лучшие-практики)

---

## Что такое Роуты? {#что-такое-роуты}

**Роут (Route)** — это адрес (URL), по которому клиент обращается к серверу. Роут определяет, какой код выполнится при определенном запросе.

### Простая аналогия:

Роут — это как адрес дома:
- `POST /auth/register` — "дом регистрации"
- `GET /chats/123` — "дом чата номер 123"
- `DELETE /messages/456` — "дом сообщения номер 456"

### Структура роута:

```javascript
router.МЕТОД('ПУТЬ', ФУНКЦИЯ_ОБРАБОТЧИК);
```

**Компоненты:**
- **МЕТОД** — HTTP метод (`GET`, `POST`, `PUT`, `DELETE`)
- **ПУТЬ** — часть URL после базового адреса
- **ФУНКЦИЯ_ОБРАБОТЧИК** — функция, которая выполнится при запросе

### Примеры из вашего проекта:

```javascript
// routes/auth.js
router.post('/register', register);        // POST /auth/register
router.post('/login', login);             // POST /auth/login
router.get('/users', getAllUsers);        // GET /auth/users
router.delete('/user/:userId', deleteAccount);  // DELETE /auth/user/123
router.put('/user/:userId/password', changePassword);  // PUT /auth/user/123/password
```

**Что происходит:**
- Когда клиент отправляет `POST /auth/register` → выполняется функция `register`
- Когда клиент отправляет `GET /auth/users` → выполняется функция `getAllUsers`

---

## Что такое Контроллеры? {#что-такое-контроллеры}

**Контроллер (Controller)** — это функция, которая обрабатывает запрос. Она содержит всю бизнес-логику: работу с базой данных, проверку данных, формирование ответа.

### Простая аналогия:

Контроллер — это как официант в ресторане:
1. Принимает заказ (получает данные из запроса)
2. Идет на кухню (работает с базой данных)
3. Приносит готовое блюдо (отправляет ответ клиенту)

### Структура контроллера:

```javascript
export const имяФункции = async (req, res) => {
  // 1. Получаем данные из запроса
  const данные = req.body;  // или req.params, req.query
  
  // 2. Проверяем и валидируем данные
  if (!данные) {
    return res.status(400).json({ message: 'Ошибка' });
  }
  
  // 3. Выполняем операции (работа с БД, вычисления)
  const результат = await pool.query('SELECT ...');
  
  // 4. Отправляем ответ клиенту
  res.status(200).json(результат);
};
```

### Пример из вашего проекта:

```javascript
// controllers/authController.js
export const register = async (req, res) => {
  // 1. Получаем данные из тела запроса
  const { email, password } = req.body;
  
  // 2. Проверяем, что данные есть
  if (!email || !password) {
    return res.status(400).json({ 
      message: 'Email и пароль обязательны' 
    });
  }
  
  // 3. Проверяем, не существует ли уже такой пользователь
  const existing = await pool.query(
    'SELECT 1 FROM users WHERE email = $1', 
    [email]
  );
  
  if (existing.rows.length > 0) {
    return res.status(400).json({ 
      message: 'Пользователь уже существует' 
    });
  }
  
  // 4. Создаем нового пользователя в базе данных
  const result = await pool.query(
    'INSERT INTO users (email, password) VALUES ($1, $2) RETURNING id, email',
    [email, password]
  );
  
  // 5. Отправляем успешный ответ
  res.status(201).json({
    userId: result.rows[0].id,
    email: result.rows[0].email,
  });
};
```

**Что делает этот контроллер:**
1. ✅ Получает email и password из запроса
2. ✅ Проверяет, что они не пустые
3. ✅ Проверяет, нет ли уже такого пользователя
4. ✅ Создает нового пользователя в БД
5. ✅ Возвращает данные нового пользователя

---

## Как они работают вместе {#как-они-работают-вместе}

### Полный путь запроса:

```
┌─────────────────────────────────────────────────────────────┐
│ 1. КЛИЕНТ (Flutter приложение)                              │
│    Отправляет запрос: POST https://server.com/auth/register │
│    С данными: { email: "user@example.com", password: "123" }│
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. СЕРВЕР (Express.js)                                      │
│    index.js получает запрос                                 │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. РОУТЕР (routes/auth.js)                                  │
│    app.use('/auth', authRoutes)                             │
│    → Направляет в authRoutes                                │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. РОУТ (routes/auth.js)                                    │
│    router.post('/register', register)                       │
│    → Находит роут /register                                 │
│    → Вызывает функцию register                               │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. КОНТРОЛЛЕР (controllers/authController.js)               │
│    export const register = async (req, res) => { ... }       │
│    → Выполняет логику регистрации                           │
│    → Работает с базой данных                                │
│    → Отправляет ответ                                        │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. ОТВЕТ КЛИЕНТУ                                            │
│    { userId: 123, email: "user@example.com" }               │
└─────────────────────────────────────────────────────────────┘
```

### Реальный пример из вашего кода:

**Шаг 1: Главный файл сервера (`index.js`)**

```javascript
import authRoutes from './routes/auth.js';
import chatRoutes from './routes/chats.js';
import messageRoutes from './routes/messages.js';

// Подключаем роуты к приложению
app.use('/auth', authRoutes);      // Все запросы /auth/* → authRoutes
app.use('/chats', chatRoutes);     // Все запросы /chats/* → chatRoutes
app.use('/messages', messageRoutes); // Все запросы /messages/* → messageRoutes
```

**Что это значит:**
- Запрос `POST /auth/register` → идет в `authRoutes`
- Запрос `GET /chats/123` → идет в `chatRoutes`
- Запрос `POST /messages` → идет в `messageRoutes`

**Шаг 2: Файл роутов (`routes/auth.js`)**

```javascript
import express from 'express';
import { register, login, getAllUsers, deleteAccount, changePassword } 
  from '../controllers/authController.js';

const router = express.Router();

// Определяем роуты и связываем их с контроллерами
router.post('/register', register);              // POST /auth/register → register()
router.post('/login', login);                    // POST /auth/login → login()
router.get('/users', getAllUsers);              // GET /auth/users → getAllUsers()
router.delete('/user/:userId', deleteAccount);  // DELETE /auth/user/123 → deleteAccount()
router.put('/user/:userId/password', changePassword); // PUT /auth/user/123/password → changePassword()

export default router;
```

**Что происходит:**
- `router.post('/register', register)` означает:
  - Когда приходит `POST /auth/register`
  - Вызвать функцию `register` из контроллера

**Шаг 3: Контроллер (`controllers/authController.js`)**

```javascript
export const register = async (req, res) => {
  // req - объект запроса (содержит данные от клиента)
  // res - объект ответа (для отправки данных клиенту)
  
  const { email, password } = req.body;  // Получаем данные из тела запроса
  
  // ... логика регистрации ...
  
  res.status(201).json({ userId: 123, email: email });  // Отправляем ответ
};
```

---

## Детальный разбор на примерах {#детальный-разбор}

### Пример 1: Регистрация пользователя

**Роут:**
```javascript
// routes/auth.js
router.post('/register', register);
```

**Контроллер:**
```javascript
// controllers/authController.js
export const register = async (req, res) => {
  // 1. Получаем данные из запроса
  const { email, password } = req.body;
  // req.body = { email: "user@example.com", password: "123" }
  
  // 2. Валидация (проверка данных)
  if (!email || !password) {
    return res.status(400).json({ 
      message: 'Email и пароль обязательны' 
    });
    // Если данных нет - отправляем ошибку и выходим
  }
  
  // 3. Проверка существования пользователя
  const existing = await pool.query(
    'SELECT 1 FROM users WHERE email = $1', 
    [email]
  );
  
  if (existing.rows.length > 0) {
    return res.status(400).json({ 
      message: 'Пользователь уже существует' 
    });
  }
  
  // 4. Создание пользователя в БД
  const result = await pool.query(
    'INSERT INTO users (email, password) VALUES ($1, $2) RETURNING id, email',
    [email, password]
  );
  
  // 5. Успешный ответ
  res.status(201).json({
    userId: result.rows[0].id,
    email: result.rows[0].email,
  });
};
```

**Как это работает:**
1. Клиент отправляет: `POST /auth/register` с `{ email: "...", password: "..." }`
2. Роут направляет запрос в функцию `register`
3. Контроллер проверяет данные, создает пользователя, возвращает результат

---

### Пример 2: Получение чатов пользователя

**Роут:**
```javascript
// routes/chats.js
router.get('/:id', getUserChats);
// GET /chats/123 → получить чаты пользователя с ID 123
```

**Контроллер:**
```javascript
// controllers/chatsController.js
export const getUserChats = async (req, res) => {
  const userId = req.params.id;  // Получаем ID из URL
  // Если URL: /chats/123, то userId = "123"
  
  try {
    // Запрос к базе данных
    const result = await pool.query(`
      SELECT c.* FROM chats c
      JOIN chat_users cu ON c.id = cu.chat_id
      WHERE cu.user_id = $1
    `, [userId]);
    
    // Отправляем список чатов
    res.json(result.rows);
  } catch (error) {
    // Обработка ошибок
    res.status(500).json({ message: 'Ошибка сервера' });
  }
};
```

**Как это работает:**
1. Клиент отправляет: `GET /chats/123`
2. Роут извлекает `123` как параметр `:id`
3. Контроллер получает `req.params.id = "123"`
4. Контроллер делает запрос к БД и возвращает чаты

---

### Пример 3: Удаление сообщения

**Роут:**
```javascript
// routes/messages.js
router.delete('/message/:messageId', deleteMessage);
// DELETE /messages/message/456 → удалить сообщение с ID 456
```

**Контроллер:**
```javascript
// controllers/messagesController.js
export const deleteMessage = async (req, res) => {
  const messageId = req.params.messageId;  // Из URL
  const userId = req.body.userId || req.query.userId;  // Из тела или query
  
  // Проверяем, существует ли сообщение
  const messageCheck = await pool.query(
    'SELECT id, user_id, chat_id FROM messages WHERE id = $1',
    [messageId]
  );
  
  if (messageCheck.rows.length === 0) {
    return res.status(404).json({ message: 'Сообщение не найдено' });
  }
  
  const message = messageCheck.rows[0];
  
  // Проверяем права (только автор может удалить)
  if (userId && userId.toString() !== message.user_id.toString()) {
    return res.status(403).json({
      message: 'Вы не являетесь отправителем этого сообщения'
    });
  }
  
  // Удаляем сообщение
  await pool.query('DELETE FROM messages WHERE id = $1', [messageId]);
  
  // Отправляем уведомление через WebSocket
  // ... код WebSocket ...
  
  res.status(200).json({ message: 'Сообщение успешно удалено' });
};
```

---

## HTTP методы {#http-методы}

HTTP методы определяют тип операции:

### GET — Получить данные

```javascript
router.get('/users', getAllUsers);
// GET /auth/users → получить список пользователей
```

**Использование:**
- Получение списка
- Получение одного объекта
- Поиск

**Особенности:**
- Данные передаются в URL (query параметры)
- Не имеет тела запроса (body)
- Безопасный (не изменяет данные)

### POST — Создать данные

```javascript
router.post('/register', register);
// POST /auth/register → создать нового пользователя
```

**Использование:**
- Создание новых записей
- Отправка форм
- Выполнение действий

**Особенности:**
- Данные передаются в теле запроса (body)
- Может изменять данные на сервере

### PUT — Обновить данные

```javascript
router.put('/user/:userId/password', changePassword);
// PUT /auth/user/123/password → изменить пароль пользователя 123
```

**Использование:**
- Обновление существующих данных
- Замена данных

**Особенности:**
- Данные передаются в теле запроса
- Изменяет данные на сервере

### DELETE — Удалить данные

```javascript
router.delete('/user/:userId', deleteAccount);
// DELETE /auth/user/123 → удалить пользователя 123
```

**Использование:**
- Удаление записей
- Отмена действий

**Особенности:**
- Может иметь тело запроса (но обычно не используется)
- Изменяет данные на сервере

---

## Параметры запросов {#параметры-запросов}

### 1. Параметры URL (`req.params`)

Параметры в пути URL:

```javascript
// Роут
router.get('/chats/:id', getUserChats);
// GET /chats/123

// Контроллер
export const getUserChats = async (req, res) => {
  const chatId = req.params.id;  // chatId = "123"
};
```

**Примеры:**
- `/chats/:id` → `req.params.id`
- `/user/:userId/password` → `req.params.userId`
- `/messages/message/:messageId` → `req.params.messageId`

### 2. Тело запроса (`req.body`)

Данные, отправленные в теле запроса (для POST, PUT):

```javascript
// Клиент отправляет
POST /auth/register
Body: { "email": "user@example.com", "password": "123" }

// Контроллер получает
export const register = async (req, res) => {
  const { email, password } = req.body;
  // email = "user@example.com"
  // password = "123"
};
```

### 3. Query параметры (`req.query`)

Параметры после `?` в URL:

```javascript
// Запрос
GET /chats/123?userId=456&sort=date

// Контроллер
export const getUserChats = async (req, res) => {
  const userId = req.query.userId;  // "456"
  const sort = req.query.sort;       // "date"
};
```

### Комбинированный пример:

```javascript
// Роут
router.delete('/user/:userId', deleteAccount);
// DELETE /auth/user/123?confirm=true

// Контроллер
export const deleteAccount = async (req, res) => {
  const userId = req.params.userId;        // "123" из URL
  const password = req.body.password;      // из тела запроса
  const confirm = req.query.confirm;       // "true" из query
};
```

---

## Обработка ошибок {#обработка-ошибок}

### Стандартные HTTP коды статуса:

- **200** — Успешно
- **201** — Создано успешно
- **400** — Неверный запрос (валидация)
- **401** — Не авторизован (неверный пароль)
- **403** — Доступ запрещен (нет прав)
- **404** — Не найдено
- **500** — Ошибка сервера

### Примеры обработки ошибок:

**Валидация данных:**
```javascript
if (!email || !password) {
  return res.status(400).json({ 
    message: 'Email и пароль обязательны' 
  });
}
```

**Проверка существования:**
```javascript
const userCheck = await pool.query(
  'SELECT id FROM users WHERE id = $1',
  [userId]
);

if (userCheck.rows.length === 0) {
  return res.status(404).json({ 
    message: 'Пользователь не найден' 
  });
}
```

**Проверка прав доступа:**
```javascript
if (userId !== creatorId) {
  return res.status(403).json({
    message: 'Только создатель чата может его удалить'
  });
}
```

**Обработка ошибок БД:**
```javascript
try {
  const result = await pool.query('SELECT ...');
  res.json(result.rows);
} catch (error) {
  console.error('Ошибка:', error);
  res.status(500).json({ 
    message: 'Ошибка сервера',
    error: error.message 
  });
}
```

---

## Лучшие практики {#лучшие-практики}

### 1. Разделение ответственности

**✅ Хорошо:**
```javascript
// Роут только маршрутизирует
router.post('/register', register);

// Контроллер содержит всю логику
export const register = async (req, res) => {
  // Вся логика здесь
};
```

**❌ Плохо:**
```javascript
// Не смешивайте логику в роутах
router.post('/register', async (req, res) => {
  // Логика регистрации здесь - плохо!
});
```

### 2. Именование

**Роуты:**
- Используйте существительные: `/users`, `/chats`, `/messages`
- Используйте глаголы для действий: `/login`, `/register`

**Контроллеры:**
- Используйте глаголы: `getUserChats`, `createChat`, `deleteMessage`
- Будьте конкретными: не `getData`, а `getUserChats`

### 3. Валидация

**Всегда проверяйте данные:**
```javascript
export const register = async (req, res) => {
  const { email, password } = req.body;
  
  // Проверка обязательных полей
  if (!email || !password) {
    return res.status(400).json({ 
      message: 'Email и пароль обязательны' 
    });
  }
  
  // Проверка формата
  if (!email.includes('@')) {
    return res.status(400).json({ 
      message: 'Неверный формат email' 
    });
  }
  
  // Проверка длины
  if (password.length < 4) {
    return res.status(400).json({ 
      message: 'Пароль должен содержать минимум 4 символа' 
    });
  }
  
  // ... остальная логика
};
```

### 4. Обработка ошибок

**Всегда используйте try-catch:**
```javascript
export const getUserChats = async (req, res) => {
  try {
    // Код, который может вызвать ошибку
    const result = await pool.query('SELECT ...');
    res.json(result.rows);
  } catch (error) {
    // Логируем ошибку для отладки
    console.error('Ошибка получения чатов:', error);
    
    // Отправляем понятное сообщение клиенту
    res.status(500).json({ 
      message: 'Ошибка сервера' 
    });
  }
};
```

### 5. Использование async/await

**✅ Хорошо:**
```javascript
export const register = async (req, res) => {
  try {
    const result = await pool.query('SELECT ...');
    res.json(result.rows);
  } catch (error) {
    res.status(500).json({ message: 'Ошибка' });
  }
};
```

**❌ Плохо:**
```javascript
export const register = (req, res) => {
  pool.query('SELECT ...', (error, result) => {
    if (error) {
      res.status(500).json({ message: 'Ошибка' });
    } else {
      res.json(result.rows);
    }
  });
};
```

---

## Структура файлов в вашем проекте

```
my_serve_chat_test/
├── index.js                    # Главный файл - подключает роуты
├── routes/                     # Папка с роутами
│   ├── auth.js                # Роуты для аутентификации
│   ├── chats.js               # Роуты для чатов
│   └── messages.js            # Роуты для сообщений
└── controllers/               # Папка с контроллерами
    ├── authController.js      # Контроллеры для аутентификации
    ├── chatsController.js     # Контроллеры для чатов
    └── messagesController.js  # Контроллеры для сообщений
```

**Связь:**
- `routes/auth.js` → использует функции из `controllers/authController.js`
- `routes/chats.js` → использует функции из `controllers/chatsController.js`
- `routes/messages.js` → использует функции из `controllers/messagesController.js`

---

## Полный пример: Смена пароля

Давайте разберем весь процесс на примере смены пароля:

### 1. Клиент (Flutter) отправляет запрос:

```dart
// lib/services/auth_service.dart
await http.put(
  Uri.parse('$baseUrl/auth/user/$userId/password'),
  headers: {'Content-Type': 'application/json'},
  body: jsonEncode({
    'oldPassword': oldPassword,
    'newPassword': newPassword,
  }),
);
```

**Что отправляется:**
- Метод: `PUT`
- URL: `https://server.com/auth/user/123/password`
- Тело: `{ "oldPassword": "старый", "newPassword": "новый" }`

### 2. Сервер получает запрос (`index.js`):

```javascript
app.use('/auth', authRoutes);
// Запрос /auth/user/123/password → направляется в authRoutes
```

### 3. Роут находит нужный обработчик (`routes/auth.js`):

```javascript
router.put('/user/:userId/password', changePassword);
// PUT /auth/user/:userId/password → вызывает changePassword()
// :userId = "123"
```

### 4. Контроллер обрабатывает запрос (`controllers/authController.js`):

```javascript
export const changePassword = async (req, res) => {
  // 1. Получаем параметры
  const userId = req.params.userId;  // "123" из URL
  const { oldPassword, newPassword } = req.body;  // из тела запроса
  
  // 2. Валидация
  if (!oldPassword || !newPassword) {
    return res.status(400).json({ 
      message: 'Требуются старый и новый пароль' 
    });
  }
  
  if (oldPassword === newPassword) {
    return res.status(400).json({ 
      message: 'Новый пароль должен отличаться от старого' 
    });
  }
  
  // 3. Проверка текущего пароля
  const userCheck = await pool.query(
    'SELECT id FROM users WHERE id = $1 AND password = $2',
    [userId, oldPassword]
  );
  
  if (userCheck.rows.length === 0) {
    return res.status(401).json({ 
      message: 'Неверный текущий пароль' 
    });
  }
  
  // 4. Обновление пароля
  await pool.query(
    'UPDATE users SET password = $1 WHERE id = $2',
    [newPassword, userId]
  );
  
  // 5. Успешный ответ
  res.status(200).json({ 
    message: 'Пароль успешно изменен' 
  });
};
```

### 5. Клиент получает ответ:

```dart
if (response.statusCode == 200) {
  // Пароль успешно изменен!
  print('Password changed successfully');
} else {
  // Ошибка
  throw Exception('Не удалось изменить пароль');
}
```

---

## Резюме

### Роуты:
- 📍 **Что:** Адреса (URL), по которым обращаются к серверу
- 🎯 **Зачем:** Определяют, какой код выполнится при запросе
- 📝 **Где:** Папка `routes/`
- 🔗 **Как:** Связывают URL с функциями-контроллерами

### Контроллеры:
- ⚙️ **Что:** Функции, которые обрабатывают запросы
- 🎯 **Зачем:** Содержат бизнес-логику (работа с БД, валидация)
- 📝 **Где:** Папка `controllers/`
- 🔗 **Как:** Получают данные из `req`, обрабатывают, отправляют через `res`

### Связь:
```
Клиент → Роут → Контроллер → База данных → Ответ → Клиент
```

### Запомните:
1. **Роуты** = куда идти (адреса)
2. **Контроллеры** = что делать (логика)
3. Роуты **направляют** запросы к контроллерам
4. Контроллеры **обрабатывают** запросы и **отправляют** ответы

---

## Полезные ссылки

- [Express.js Routing](https://expressjs.com/en/guide/routing.html)
- [HTTP Methods](https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods)
- [REST API Best Practices](https://restfulapi.net/)

---

**Теперь вы понимаете, как работает серверная часть вашего приложения!** 🚀

