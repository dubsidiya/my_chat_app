# 🚀 Инструкция по настройке после исправлений безопасности

## ⚠️ ВАЖНО: Перед запуском

### 1. Установите новые зависимости

```bash
cd my_serve_chat_test
npm install
```

Это установит:
- `bcrypt` - для хеширования паролей
- `jsonwebtoken` - для JWT токенов
- `express-rate-limit` - для защиты от брутфорса
- `validator` - для валидации данных

### 2. Настройте переменные окружения

Создайте или обновите файл `.env` в папке `my_serve_chat_test/`:

```env
# JWT секретный ключ (ОБЯЗАТЕЛЬНО измените на случайную строку!)
JWT_SECRET=your-super-secret-random-string-min-32-chars

# Разрешенные домены для CORS (через запятую, без пробелов)
ALLOWED_ORIGINS=https://my-chat-app.vercel.app,http://localhost:3000,http://localhost:8080

# URL базы данных (уже должен быть)
DATABASE_URL=your-database-url

# Порт сервера (опционально, по умолчанию 3000)
PORT=3000
```

**⚠️ КРИТИЧНО:** 
- `JWT_SECRET` должен быть длинной случайной строкой (минимум 32 символа)
- НИКОГДА не коммитьте `.env` в Git!
- Используйте разные ключи для development и production

### 3. Миграция существующих паролей

**Проблема:** Существующие пароли в БД хранятся в открытом виде, а теперь нужны хешированные.

**Решение:**

#### Вариант 1: Попросить пользователей сбросить пароли
- После обновления все пользователи должны будут войти заново
- При первом входе пароль будет перехеширован автоматически

#### Вариант 2: Создать скрипт миграции

Создайте файл `my_serve_chat_test/migrate_passwords.js`:

```javascript
import pool from './db.js';
import bcrypt from 'bcrypt';
import dotenv from 'dotenv';

dotenv.config();

async function migratePasswords() {
  try {
    // Получаем всех пользователей с открытыми паролями
    const users = await pool.query('SELECT id, password FROM users');
    
    console.log(`Найдено ${users.rows.length} пользователей для миграции`);
    
    for (const user of users.rows) {
      // Проверяем, не хеширован ли уже пароль
      // (хеши bcrypt начинаются с $2a$, $2b$ или $2y$)
      if (user.password.startsWith('$2')) {
        console.log(`Пользователь ${user.id} уже имеет хешированный пароль`);
        continue;
      }
      
      // Хешируем пароль
      const hashedPassword = await bcrypt.hash(user.password, 10);
      
      // Обновляем в БД
      await pool.query(
        'UPDATE users SET password = $1 WHERE id = $2',
        [hashedPassword, user.id]
      );
      
      console.log(`Пароль пользователя ${user.id} перехеширован`);
    }
    
    console.log('Миграция завершена!');
    process.exit(0);
  } catch (error) {
    console.error('Ошибка миграции:', error);
    process.exit(1);
  }
}

migratePasswords();
```

Запустите:
```bash
node migrate_passwords.js
```

**⚠️ ВАЖНО:** После миграции удалите этот скрипт!

---

## 🔄 Изменения в API

### Новые требования:

1. **Все защищенные эндпоинты требуют JWT токен:**
   ```
   Authorization: Bearer <token>
   ```

2. **Регистрация и вход возвращают токен:**
   ```json
   {
     "id": 1,
     "email": "user@mail.com",
     "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
   }
   ```

3. **WebSocket требует токен:**
   ```
   wss://server.com?token=<jwt_token>
   ```

4. **userId больше не передается в запросах:**
   - Берется из JWT токена на сервере
   - Нельзя подменить!

---

## 📱 Обновление клиентской части

Клиентская часть уже обновлена:
- ✅ Сохранение токена в SharedPreferences
- ✅ Добавление токена в заголовки запросов
- ✅ Использование токена для WebSocket
- ✅ Автоматический вход при наличии токена

---

## 🧪 Тестирование

### 1. Регистрация нового пользователя
```bash
curl -X POST https://your-server.com/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@mail.com","password":"test123456"}'
```

Должен вернуть токен.

### 2. Вход в систему
```bash
curl -X POST https://your-server.com/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@mail.com","password":"test123456"}'
```

Должен вернуть токен.

### 3. Защищенный эндпоинт
```bash
curl -X GET https://your-server.com/auth/users \
  -H "Authorization: Bearer <your-token>"
```

Должен вернуть список пользователей.

### 4. Без токена (должна быть ошибка)
```bash
curl -X GET https://your-server.com/auth/users
```

Должен вернуть 401 Unauthorized.

### 5. Rate limiting
Попробуйте войти 6 раз подряд - 6-я попытка должна быть заблокирована на 15 минут.

---

## ✅ Чеклист перед деплоем

- [ ] Установлены все зависимости (`npm install`)
- [ ] Настроен `.env` с `JWT_SECRET` и `ALLOWED_ORIGINS`
- [ ] Мигрированы пароли существующих пользователей
- [ ] Протестирована регистрация
- [ ] Протестирован вход
- [ ] Протестированы защищенные эндпоинты
- [ ] Протестирован WebSocket
- [ ] Проверен rate limiting
- [ ] `.env` добавлен в `.gitignore`

---

## 🐛 Решение проблем

### Ошибка: "JWT_SECRET is not defined"
**Решение:** Добавьте `JWT_SECRET` в `.env`

### Ошибка: "CORS error"
**Решение:** Добавьте ваш домен в `ALLOWED_ORIGINS` в `.env`

### Ошибка: "Invalid token"
**Решение:** 
- Проверьте, что токен передается в заголовке `Authorization: Bearer <token>`
- Убедитесь, что токен не истек (действителен 7 дней)

### Ошибка: "Too many requests"
**Решение:** Вы превысили лимит rate limiting (5 попыток за 15 минут). Подождите.

### Пароли не работают после обновления
**Решение:** Запустите скрипт миграции паролей или попросите пользователей сбросить пароли.

---

*Готово к использованию! 🎉*

