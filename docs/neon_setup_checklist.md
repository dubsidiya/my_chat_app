# Чеклист настройки Neon PostgreSQL

## ✅ Шаг 1: Проверьте таблицы в Neon

1. Зайдите на https://console.neon.tech
2. Выберите ваш проект
3. Откройте **SQL Editor**
4. Выполните этот запрос, чтобы проверить структуру таблиц:

```sql
-- Проверка существования таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- Проверка структуры таблицы chats (должна быть колонка is_group)
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_name = 'chats'
ORDER BY ordinal_position;

-- Проверка структуры таблицы users
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_name = 'users'
ORDER BY ordinal_position;
```

**Ожидаемый результат:**
- Таблицы: `users`, `chats`, `chat_users`, `messages`
- В таблице `chats` должна быть колонка `is_group` типа `boolean`

### Если колонки `is_group` нет:

Выполните:
```sql
ALTER TABLE chats ADD COLUMN IF NOT EXISTS is_group BOOLEAN DEFAULT false;
```

### Если таблиц нет вообще:

Выполните SQL из файла `create_tables.sql`

---

## ✅ Шаг 2: Получите Connection String из Neon

1. В Neon Console выберите ваш проект
2. Перейдите в **Dashboard** → **Connection Details**
3. Выберите **"Pooled connection"** (рекомендуется для продакшена)
   - Или **"Direct connection"** (если pooling не нужен)
4. Скопируйте connection string
   - Формат: `postgresql://user:password@host.neon.tech/dbname?sslmode=require`

**Важно**: Используйте **Pooled connection** для лучшей производительности и стабильности.

---

## ✅ Шаг 3: Обновите переменные окружения на Render

1. Зайдите на https://dashboard.render.com
2. Выберите ваш сервис `my-server-chat`
3. Перейдите в раздел **Environment**
4. Найдите переменную:
   - `DATABASE_URL` или
   - `POSTGRES_URL` или
   - `DB_URL` или
   - Любую другую переменную, которая содержит connection string к БД
5. **Замените** значение на connection string из Neon (из шага 2)
6. **Сохраните** изменения

**Проверьте**: Убедитесь, что в connection string нет лишних пробелов или символов.

---

## ✅ Шаг 4: Перезапустите сервер на Render

1. В Render нажмите **"Manual Deploy"** → **"Clear build cache & deploy"**
2. Или просто перезапустите сервис через кнопку **"Restart"**

**Важно**: После изменения переменных окружения сервер нужно перезапустить!

---

## ✅ Шаг 5: Проверьте логи на Render

1. После перезапуска откройте раздел **Logs** в Render
2. Проверьте, что нет ошибок подключения к БД
3. Должны быть сообщения об успешном подключении

**Ожидаемые логи:**
- ✅ "Connected to database"
- ✅ "Database connection established"
- ❌ НЕ должно быть: "ENETUNREACH", "connection refused", "column does not exist"

---

## ✅ Шаг 6: Протестируйте приложение

Попробуйте в приложении:

1. **Регистрация нового пользователя**
   - Должна пройти успешно
   - Проверьте в Neon, что пользователь появился в таблице `users`

2. **Вход**
   - Должен работать без ошибок

3. **Создание чата**
   - Должно работать без ошибки "column is_group does not exist"
   - Проверьте в Neon, что чат появился в таблице `chats`

4. **Отправка сообщения**
   - Должна работать корректно

---

## 🔧 Если что-то не работает

### Ошибка: "column is_group does not exist"

**Решение**: Выполните в Neon SQL Editor:
```sql
ALTER TABLE chats ADD COLUMN IF NOT EXISTS is_group BOOLEAN DEFAULT false;
```

### Ошибка: "connection refused" или "ENETUNREACH"

**Решение**: 
- Проверьте, что используете правильный connection string из Neon
- Убедитесь, что используете **Pooled connection** (порт обычно 5432 или другой)
- Проверьте, что в connection string указан `?sslmode=require`

### Ошибка: "password authentication failed"

**Решение**:
- Скопируйте connection string заново из Neon
- Убедитесь, что пароль не был изменен

### Таблицы не созданы

**Решение**: Выполните SQL из файла `create_tables.sql` в Neon SQL Editor

---

## 📝 Полезные SQL запросы для проверки

```sql
-- Посмотреть всех пользователей
SELECT * FROM users;

-- Посмотреть все чаты
SELECT * FROM chats;

-- Посмотреть все сообщения
SELECT * FROM messages ORDER BY created_at DESC LIMIT 10;

-- Посмотреть связи пользователей с чатами
SELECT * FROM chat_users;
```

---

## ✅ Готово!

После выполнения всех шагов ваше приложение должно работать с Neon PostgreSQL без проблем!

