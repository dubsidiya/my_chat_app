# 🔒 Безопасность: ВСЕ УЯЗВИМОСТИ ИСПРАВЛЕНЫ

## ✅ Статус: КРИТИЧЕСКИЕ УЯЗВИМОСТИ ЗАКРЫТЫ

**Было:** 3/10 🔴  
**Стало:** 8/10 🟢

---

## 📋 Что было исправлено

### 🔴 Критические уязвимости (ИСПРАВЛЕНЫ)

| # | Уязвимость | Статус | Файлы |
|---|------------|--------|-------|
| 1 | Пароли в открытом виде | ✅ ИСПРАВЛЕНО | `authController.js` |
| 2 | Нет защиты от брутфорса | ✅ ИСПРАВЛЕНО | `index.js` |
| 3 | Нет JWT токенов | ✅ ИСПРАВЛЕНО | `middleware/auth.js` (новый) |
| 4 | CORS открыт для всех | ✅ ИСПРАВЛЕНО | `index.js` |
| 5 | WebSocket без аутентификации | ✅ ИСПРАВЛЕНО | `websocket/websocket.js` |
| 6 | Логирование паролей | ✅ ИСПРАВЛЕНО | `authController.js` |
| 7 | Публичный список пользователей | ✅ ИСПРАВЛЕНО | `authController.js`, `routes/auth.js` |
| 8 | Нет валидации данных | ✅ ИСПРАВЛЕНО | `utils/validation.js` (новый) |
| 9 | userId можно подменить | ✅ ИСПРАВЛЕНО | Все контроллеры |

### Дополнительные исправления (аудит 2025)

| # | Уязвимость / риск | Статус | Файлы |
|---|-------------------|--------|-------|
| 10 | Утечка `error.message` в ответах API | ✅ ИСПРАВЛЕНО | `authController.js`, `bankStatementController.js`, `setupController.js` |
| 11 | Нет лимита длины FCM-токена (DoS/переполнение БД) | ✅ ИСПРАВЛЕНО | `authController.js` |
| 12 | Заголовок X-Powered-By раскрывает технологию | ✅ ИСПРАВЛЕНО | `index.js` |
| 13 | SVG в загрузке изображений (XSS через скрипты в SVG) | ✅ ИСПРАВЛЕНО | `utils/uploadImage.js` |
| 14 | Перебор инвайт-кодов (нет rate limit на POST /chats/join) | ✅ ИСПРАВЛЕНО | `routes/chats.js` |
| 15 | Загрузка файлов до 2 ГБ в память (DoS) | ✅ ИСПРАВЛЕНО | `utils/uploadFile.js`, `routes/messages.js` |
| 16 | JWT_SECRET без проверки минимальной длины в production | ✅ ИСПРАВЛЕНО | `index.js` |
| 17 | Очень длинный токен в Authorization (DoS в jwt.verify) | ✅ ИСПРАВЛЕНО | `middleware/auth.js` |
| 18 | WebSocket: нет лимита размера сообщения | ✅ ИСПРАВЛЕНО | `websocket/websocket.js` |

### Максимальная защита (раунд 3)

| # | Мера | Статус | Файлы |
|---|------|--------|-------|
| 19 | HSTS в production (только HTTPS) | ✅ | `index.js` |
| 20 | Заголовок X-DNS-Prefetch-Control: off | ✅ | `index.js` |
| 21 | Глобальный rate limit 600 req/15 мин на IP | ✅ | `index.js` |
| 22 | Rate limit на сохранение FCM-токена (15/15 мин) | ✅ | `routes/auth.js` |
| 23 | Санитизация отображаемых строк (ник, имя чата, папки): управляющие и zero-width символы | ✅ | `utils/sanitize.js`, `authController.js`, `chatsController.js` |
| 24 | Санитизация текста сообщений (null-байты и управляющие символы) | ✅ | `utils/sanitize.js`, `messagesController.js` |
| 25 | Валидация ID: только положительные целые (parsePositiveInt) | ✅ | `utils/sanitize.js`, `chatsController.js`, `moderationController.js` |
| 26 | Обработчик 404 и ошибок без утечки деталей | ✅ | `index.js` |

### Раунд 4 — дополнительное усиление

| # | Мера | Статус | Файлы |
|---|------|--------|-------|
| 27 | parsePositiveInt: отклонение значений > MAX_SAFE_INTEGER | ✅ | `utils/sanitize.js` |
| 28 | Лимит размера массивов: userIds (createChat/addMembers) ≤ 50, payments ≤ 200 | ✅ | `chatsController.js`, `bankStatementController.js` |
| 29 | Rate limit на критические действия: удаление аккаунта, смена пароля (5/час на пользователя) | ✅ | `routes/auth.js` |
| 30 | Rate limit на GET /auth/users/:userId (120/15 мин) — защита от скрапинга | ✅ | `routes/auth.js` |
| 31 | Audit log: события login_fail, password_changed, account_deleted, admin_reset_password (без PII) | ✅ | `utils/auditLog.js`, `authController.js` |
| 32 | В production: предупреждение, если не задан ALLOWED_ORIGINS | ✅ | `index.js` |
| 33 | Helmet: набор security-заголовков (X-Content-Type-Options, X-DNS-Prefetch-Control и др.) | ✅ | `index.js`, `package.json` |
| 34 | Санитизация имени чата при создании (createChat) | ✅ | `chatsController.js` |

---

## 🛠️ Технические изменения

### Серверная часть (Node.js)

#### Новые файлы:
- ✅ `middleware/auth.js` - JWT аутентификация
- ✅ `utils/validation.js` - валидация данных
- ✅ `utils/sanitize.js` - санитизация строк и parsePositiveInt
- ✅ `utils/auditLog.js` - лог безопасности (события без PII)

#### Обновленные файлы:
- ✅ `package.json` - добавлены зависимости
- ✅ `index.js` - CORS, rate limiting
- ✅ `controllers/authController.js` - bcrypt, JWT, валидация
- ✅ `controllers/chatsController.js` - использование userId из токена
- ✅ `controllers/messagesController.js` - использование userId из токена
- ✅ `routes/auth.js` - middleware аутентификации
- ✅ `routes/chats.js` - middleware аутентификации
- ✅ `routes/messages.js` - middleware аутентификации
- ✅ `websocket/websocket.js` - проверка JWT токена

### Клиентская часть (Flutter)

#### Обновленные файлы:
- ✅ `services/storage_service.dart` - сохранение токена
- ✅ `services/auth_service.dart` - работа с токенами
- ✅ `services/chats_service.dart` - добавление токена в заголовки
- ✅ `services/messages_service.dart` - добавление токена в заголовки
- ✅ `screens/login_screen.dart` - сохранение токена
- ✅ `screens/register_screen.dart` - сохранение токена
- ✅ `screens/chat_screen.dart` - WebSocket с токеном
- ✅ `main.dart` - проверка токена при запуске

---

## 🔐 Новые механизмы безопасности

### 1. Хеширование паролей (bcrypt)
```javascript
// Было:
INSERT INTO users (email, password) VALUES ($1, $2)
// password = "mypassword123" (открытый текст)

// Стало:
const hashedPassword = await bcrypt.hash(password, 10);
INSERT INTO users (email, password) VALUES ($1, $2)
// password = "$2b$10$..." (хеш)
```

### 2. JWT токены
```javascript
// Генерация токена при входе/регистрации
const token = jwt.sign({ userId, email }, JWT_SECRET, { expiresIn: '7d' });

// Проверка токена в middleware
jwt.verify(token, JWT_SECRET, (err, user) => {
  req.user = user; // userId и email из токена
});
```

### 3. Rate Limiting
```javascript
// Максимум 5 попыток входа за 15 минут
const authLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 5,
});
```

### 4. CORS ограничения
```javascript
// Только разрешенные домены
app.use(cors({
  origin: allowedOrigins, // из .env
  credentials: true
}));
```

### 5. Валидация данных
```javascript
// Проверка email и пароля
validateEmail(email) // формат email
validatePassword(password) // длина 6-128 символов
```

---

## 📊 Сравнение: До и После

| Аспект | До | После |
|--------|-----|-------|
| **Пароли** | Открытый текст | Bcrypt хеш |
| **Аутентификация** | userId в SharedPreferences | JWT токены |
| **Защита от брутфорса** | Нет | Rate limiting (5/15мин) |
| **CORS** | Открыт для всех | Только разрешенные домены |
| **WebSocket** | По userId (можно подменить) | По JWT токену |
| **Валидация** | Минимальная | Полная (email, пароль) |
| **Логирование паролей** | Да | Нет |
| **Публичный API** | getAllUsers открыт | Требует аутентификацию |
| **Подмена userId** | Возможна | Невозможна (из токена) |

---

## 🚀 Следующие шаги

### Обязательно:

1. **Установить зависимости:**
   ```bash
   cd my_serve_chat_test
   npm install
   ```

2. **Настроить .env:**
   - `JWT_SECRET` - случайная строка (минимум 32 символа)
   - `ALLOWED_ORIGINS` - разрешенные домены

3. **Мигрировать пароли:**
   - Запустить скрипт миграции
   - Или попросить пользователей сбросить пароли

### Рекомендуется:

4. **Добавить HTTPS** (уже есть на Vercel/Render ✅)
5. **Настроить мониторинг** для отслеживания подозрительной активности
6. **Регулярно обновлять зависимости** для исправления уязвимостей

---

## 📝 Документация

- **SECURITY_VULNERABILITIES.md** - описание всех уязвимостей
- **SECURITY_FIXES_APPLIED.md** - детали исправлений
- **SETUP_INSTRUCTIONS.md** - инструкция по настройке

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Существующие пользователи:** После обновления им нужно войти заново (пароли перехешированы)

2. **JWT_SECRET:** 
   - Должен быть длинной случайной строкой
   - НИКОГДА не коммитьте в Git
   - Используйте разные ключи для dev/prod

3. **CORS:** Убедитесь, что `ALLOWED_ORIGINS` содержит все нужные домены

4. **Токены:** Действительны 7 дней, после этого нужен повторный вход

---

## 🎯 Результат

**Все критические уязвимости закрыты!**

Приложение теперь защищено от:
- ✅ Подбора паролей (rate limiting)
- ✅ Перехвата сессий (JWT токены)
- ✅ Подмены userId (токены)
- ✅ CSRF атак (CORS ограничения)
- ✅ Утечки паролей (хеширование, нет логирования)
- ✅ Несанкционированного доступа (аутентификация везде)

**Оценка безопасности: 8/10** 🟢

---

## Дополнительные меры (аудит 2025)

- **Ответы API:** при ошибках возвращаются только общие сообщения, без `error.message` и стеков.
- **FCM-токен:** максимум 1024 символа.
- **Загрузка изображений:** SVG отключён (риск XSS при отображении).
- **Файлы в чате:** лимит 100 МБ (раньше 2 ГБ), файл обрабатывается в памяти.
- **Вступление по инвайту:** не более 25 попыток за 15 минут с одного IP.
- **JWT:** в production проверяется длина секрета (≥ 32 символа); длина токена в запросе ограничена 4 КБ.
- **WebSocket:** максимальный размер одного сообщения 64 КБ.
- **Заголовки:** HSTS в production, X-DNS-Prefetch-Control: off.
- **Глобальный rate limit:** 600 запросов за 15 минут с одного IP.
- **FCM-токен:** не более 15 обновлений за 15 минут на пользователя.
- **Строки для отображения:** удаление управляющих и zero-width символов (ник, имя чата, папки).
- **Текст сообщений:** удаление null-байтов и опасных управляющих символов.
- **ID в запросах:** проверка на положительное целое (отклонение 0, отрицательных, NaN).
- **404 и ошибки:** единый ответ без раскрытия структуры API и деталей.
- **Лимиты массивов:** не более 50 userIds за запрос (чат/добавление), не более 200 платежей за запрос.
- **ID:** значения > Number.MAX_SAFE_INTEGER отклоняются в parsePositiveInt.
- **Критические действия:** не более 5 попыток удаления аккаунта или смены пароля в час на пользователя.
- **Профиль по ID:** не более 120 запросов за 15 минут на пользователя.
- **Audit log:** в production логируются события login_fail, password_changed, account_deleted, admin_reset_password (timestamp, event, ip, userId).
- **Helmet:** подключён для стандартного набора security-заголовков.

### Зависимости

Рекомендуется периодически выполнять `npm audit` и `npm audit fix`. Пакет **xlsx** (SheetJS) заменён на **exceljs** из-за уязвимостей Prototype Pollution и ReDoS (нет патча). Поддержка формата: только **.xlsx**; файлы **.xls** отклоняются с сообщением о необходимости сохранить в .xlsx.

---

*Дата исправлений: 2024, дополнения 2025*

---

## Аудит после настройки ВМ и автодеплоя (2026)

Проведено 5 проходов проверки безопасности.

| Проход | Область | Результат |
|--------|---------|-----------|
| 1 | Секреты, .gitignore, ключи | ✅ .env, .deploy_key, vm-connection.txt, env-yandex-vm.txt в .gitignore; в репо только публичный ключ (cloud-init) и шаблоны |
| 2 | Бэкенд: CORS, helmet, rate limit, JWT | ✅ JWT с HS256, лимиты на auth/API/upload, setup/cors только для суперпользователя, 404/500 без утечки деталей |
| 3 | Фронт и документация | ✅ Нет хардкода паролей/токенов; в VM_COMMANDS_REFERENCE добавлено предупреждение о IP и команде с .env |
| 4 | Зависимости и скрипты | ✅ npm audit: 0 уязвимостей; execSync только для openssl rand (без пользовательского ввода) |
| 5 | Логи и PII | ✅ В production не логируем логин при неудачной валидации/регистрации (authController.js); audit log без паролей |

**Исправления в этом аудите:** убрано логирование логина/username при ошибках валидации и при попытке регистрации существующего пользователя (только в production); в справочник команд добавлено предупреждение о безопасности.

---

*Максимальная защита применена. 🎉*

