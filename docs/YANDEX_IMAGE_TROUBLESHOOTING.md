# 🔍 Диагностика проблемы с отображением изображений

## Текущая ситуация

✅ Изображение загружается в Яндекс Object Storage  
✅ CORS настроен  
❌ Изображение не отображается в чате  

## Что проверить

### 1. Публичный доступ к бакету (ВАЖНО!)

1. Откройте консоль Яндекс Облака
2. Перейдите в **Object Storage** → ваш бакет (`mychatimage`)
3. Проверьте **"Тип доступа"**:
   - ✅ Должен быть **"Публичный"**
   - ❌ Если "Приватный" - измените на "Публичный"

**Как изменить:**
- В настройках бакета найдите "Тип доступа"
- Выберите "Публичный"
- Сохраните изменения

### 2. Проверка URL изображения

1. Откройте логи на Render.com
2. Найдите строку: `Image uploaded successfully: https://...`
3. Скопируйте URL (например: `https://mychatimage.storage.yandexcloud.net/images/image-xxx.jpg`)
4. Откройте этот URL **напрямую в браузере**:
   - ✅ Если изображение открывается - URL правильный, проблема в CORS или отображении
   - ❌ Если ошибка 403/404 - проблема с публичным доступом
   - ❌ Если ошибка CORS - CORS настроен неправильно

### 3. Проверка в консоли браузера

1. Откройте DevTools (F12) в браузере
2. Перейдите на вкладку **Console**
3. Попробуйте загрузить изображение
4. Найдите ошибки:
   - `CORS policy` - проблема с CORS
   - `403 Forbidden` - проблема с публичным доступом
   - `404 Not Found` - неправильный URL
   - `Failed to load image` - общая ошибка загрузки

### 4. Проверка Network запросов

1. В DevTools перейдите на вкладку **Network**
2. Обновите страницу или загрузите новое изображение
3. Найдите запрос к изображению (фильтр: Img)
4. Проверьте:
   - **Status**: должен быть `200 OK`
   - **Headers**: проверьте `Access-Control-Allow-Origin`
   - **Response**: должно быть изображение

## Быстрая проверка

Выполните в консоли браузера (F12 → Console):

```javascript
// Замените URL на реальный URL вашего изображения
const imgUrl = 'https://mychatimage.storage.yandexcloud.net/images/image-1767024374712-854212444.jpg';

// Проверка загрузки
fetch(imgUrl, { method: 'HEAD' })
  .then(r => {
    console.log('✅ Status:', r.status);
    console.log('✅ CORS Header:', r.headers.get('Access-Control-Allow-Origin'));
    console.log('✅ Content-Type:', r.headers.get('Content-Type'));
  })
  .catch(e => console.error('❌ Error:', e));

// Проверка отображения
const img = new Image();
img.onload = () => console.log('✅ Image loaded successfully');
img.onerror = (e) => console.error('❌ Image load error:', e);
img.src = imgUrl;
```

## Решения

### Решение 1: Убедиться, что бакет публичный

1. В консоли Яндекс Облака
2. Object Storage → ваш бакет
3. Тип доступа → **"Публичный"**
4. Сохраните

### Решение 2: Проверить CORS еще раз

Убедитесь, что CORS настроен правильно:
- **Allowed Origins**: `*` или конкретные домены
- **Allowed Methods**: `GET, HEAD, OPTIONS`
- **MaxAgeSeconds**: `3600`

### Решение 3: Использовать автоматическую настройку

После деплоя изменений сервер автоматически настроит CORS при запуске.

### Решение 4: Проверить формат URL

Убедитесь, что URL правильный:
- Формат: `https://mychatimage.storage.yandexcloud.net/images/image-xxx.jpg`
- Должен начинаться с `https://`
- Должен содержать `/images/` в пути

## После исправления

1. Обновите страницу в браузере (Ctrl+F5 или Cmd+Shift+R)
2. Попробуйте загрузить новое изображение
3. Старые изображения должны начать отображаться

## Если ничего не помогает

Пришлите:
1. Скриншот ошибки из консоли браузера (F12 → Console)
2. Скриншот Network запроса к изображению (F12 → Network → Img)
3. URL изображения из логов Render.com
4. Результат проверки URL напрямую в браузере

