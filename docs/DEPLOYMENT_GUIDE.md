# Руководство по запуску приложения на компьютере

## 🌐 Вариант 1: Веб-версия (самый простой)

### Способ 1.1: Локальный запуск

1. **Установите Flutter** (если еще не установлен):
   ```bash
   # Проверьте установку
   flutter doctor
   ```

2. **Клонируйте или скачайте проект**

3. **Установите зависимости**:
   ```bash
   cd my_chat_app
   flutter pub get
   ```

4. **Запустите в браузере**:
   ```bash
   flutter run -d chrome
   ```
   
   Приложение откроется автоматически в браузере по адресу `http://localhost:xxxxx`

### Способ 1.2: Сборка для веба и размещение на хостинге

#### Шаг 1: Соберите приложение для веба
```bash
cd my_chat_app
flutter build web
```

Файлы будут созданы в папке `build/web/`

#### Шаг 2: Разместите на хостинге

**Вариант A: GitHub Pages (бесплатно)**
1. Создайте репозиторий на GitHub
2. Загрузите содержимое папки `build/web/` в репозиторий
3. Включите GitHub Pages в настройках репозитория
4. Приложение будет доступно по адресу: `https://ваш-username.github.io/репозиторий/`

**Вариант B: Netlify (бесплатно)**
1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Перетащите папку `build/web/` на сайт Netlify
3. Получите бесплатный домен

**Вариант C: Vercel (бесплатно)**
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Укажите папку `build/web` как корневую
4. Автоматический деплой при каждом обновлении

**Вариант D: Firebase Hosting (бесплатно)**
```bash
# Установите Firebase CLI
npm install -g firebase-tools

# Войдите в Firebase
firebase login

# Инициализируйте проект
firebase init hosting

# Укажите папку build/web как публичную

# Задеплойте
firebase deploy
```

**Вариант E: Render (бесплатно)**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте Static Site
3. Подключите GitHub репозиторий
4. Укажите команду сборки: `flutter build web`
5. Укажите папку: `build/web`

---

## 💻 Вариант 2: Десктопная версия (Windows/Mac/Linux)

### Для Windows:

#### Шаг 1: Установите Flutter
1. Скачайте Flutter SDK с [flutter.dev](https://flutter.dev)
2. Распакуйте в `C:\src\flutter`
3. Добавьте в PATH: `C:\src\flutter\bin`

#### Шаг 2: Установите Visual Studio
- Скачайте [Visual Studio Community](https://visualstudio.microsoft.com)
- При установке выберите "Desktop development with C++"

#### Шаг 3: Соберите приложение
```bash
cd my_chat_app
flutter pub get
flutter build windows
```

#### Шаг 4: Запустите
Файлы будут в `build/windows/runner/Release/`
Запустите `my_chat_app.exe`

### Для macOS:

#### Шаг 1: Установите Flutter
```bash
# Через Homebrew
brew install flutter

# Или скачайте с flutter.dev
```

#### Шаг 2: Соберите приложение
```bash
cd my_chat_app
flutter pub get
flutter build macos
```

#### Шаг 3: Запустите
Файлы будут в `build/macos/Build/Products/Release/`
Запустите `my_chat_app.app`

### Для Linux:

#### Шаг 1: Установите зависимости
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev

# Fedora
sudo dnf install clang cmake ninja-build pkg-config gtk3-devel
```

#### Шаг 2: Соберите приложение
```bash
cd my_chat_app
flutter pub get
flutter build linux
```

#### Шаг 3: Запустите
Файлы будут в `build/linux/x64/release/bundle/`
Запустите `my_chat_app`

---

## 📦 Вариант 3: Создание установщика

### Windows (MSI/EXE):

Используйте инструменты вроде:
- **Inno Setup** - создание EXE установщика
- **WiX Toolset** - создание MSI установщика

### macOS (DMG):

```bash
# После сборки создайте DMG
# Используйте Disk Utility или create-dmg
npm install -g create-dmg
create-dmg my_chat_app.dmg build/macos/Build/Products/Release/my_chat_app.app
```

### Linux (DEB/RPM):

```bash
# DEB (Ubuntu/Debian)
# Используйте debuild или создайте .deb пакет вручную

# RPM (Fedora/RedHat)
# Используйте rpmbuild
```

---

## 🚀 Быстрый старт для других пользователей

### Если они хотят запустить локально:

1. **Установите Flutter**:
   - Windows: скачайте с flutter.dev
   - Mac: `brew install flutter`
   - Linux: следуйте инструкциям на flutter.dev

2. **Клонируйте проект**:
   ```bash
   git clone <ваш-репозиторий>
   cd my_chat_app
   ```

3. **Установите зависимости**:
   ```bash
   flutter pub get
   ```

4. **Запустите**:
   ```bash
   # Веб-версия
   flutter run -d chrome
   
   # Или десктопная версия
   flutter run -d windows  # Windows
   flutter run -d macos    # macOS
   flutter run -d linux    # Linux
   ```

---

## 🌍 Размещение веб-версии (рекомендуется)

Самый простой способ для других пользователей - это веб-версия:

1. **Соберите приложение**:
   ```bash
   flutter build web
   ```

2. **Загрузите на хостинг** (выберите один):
   - GitHub Pages (бесплатно)
   - Netlify (бесплатно)
   - Vercel (бесплатно)
   - Firebase Hosting (бесплатно)
   - Render Static Site (бесплатно)

3. **Поделитесь ссылкой** - пользователи смогут открыть в любом браузере

---

## ⚙️ Настройка для продакшена

### Вариант 1: Веб-версия

1. **Соберите оптимизированную версию**:
   ```bash
   flutter build web --release
   ```

2. **Настройте базовый URL** (если нужно):
   Отредактируйте `web/index.html`:
   ```html
   <base href="/">
   ```

### Вариант 2: Десктопная версия

1. **Соберите release версию**:
   ```bash
   flutter build windows --release
   flutter build macos --release
   flutter build linux --release
   ```

2. **Подпишите приложение** (для распространения):
   - Windows: Code Signing Certificate
   - macOS: Apple Developer Certificate
   - Linux: обычно не требуется

---

## 📝 Чеклист для запуска

- [ ] Flutter установлен и настроен
- [ ] Зависимости установлены (`flutter pub get`)
- [ ] Сервер запущен и доступен (Render)
- [ ] База данных настроена (Neon)
- [ ] Приложение собрано (`flutter build web/windows/macos/linux`)
- [ ] Приложение протестировано
- [ ] Размещено на хостинге (для веба) или упаковано (для десктопа)

---

## 🔗 Полезные ссылки

- [Flutter Web Deployment](https://docs.flutter.dev/deployment/web)
- [Flutter Desktop Deployment](https://docs.flutter.dev/deployment/desktop)
- [GitHub Pages](https://pages.github.com)
- [Netlify](https://netlify.com)
- [Vercel](https://vercel.com)
- [Firebase Hosting](https://firebase.google.com/docs/hosting)

---

## 💡 Рекомендация

**Для быстрого запуска другими пользователями лучше всего использовать веб-версию:**
1. Соберите: `flutter build web`
2. Загрузите на бесплатный хостинг (Netlify, Vercel, GitHub Pages)
3. Поделитесь ссылкой

Это самый простой способ - пользователям не нужно ничего устанавливать, просто открыть ссылку в браузере!

