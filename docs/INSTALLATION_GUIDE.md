# Руководство по установке приложения на iPhone и Android

## 📱 Общие требования

- Flutter SDK установлен
- Android Studio (для Android)
- Xcode (для iOS, только на Mac)
- Подключенное устройство или эмулятор

---

## 🤖 Установка на Android

### Вариант 1: Установка через USB (для разработки)

#### Шаг 1: Подготовка устройства
1. На Android устройстве включите **Режим разработчика**:
   - Откройте **Настройки** → **О телефоне**
   - Нажмите 7 раз на **Номер сборки**
   - Появится сообщение "Вы стали разработчиком"

2. Включите **Отладку по USB**:
   - Откройте **Настройки** → **Для разработчиков**
   - Включите **Отладка по USB**

#### Шаг 2: Подключение устройства
1. Подключите Android устройство к компьютеру через USB
2. Разрешите отладку по USB на устройстве (появится диалог)
3. Проверьте подключение:
   ```bash
   flutter devices
   ```
   Должно показать ваше устройство

#### Шаг 3: Сборка и установка
```bash
# Перейдите в папку проекта
cd /Users/vladkharin/my_chat_app

# Установите зависимости
flutter pub get

# Запустите приложение на подключенном устройстве
flutter run
```

### Вариант 2: Создание APK файла

#### Шаг 1: Создание APK
```bash
# Создать APK для тестирования (debug)
flutter build apk --debug

# Или создать APK для релиза (release)
flutter build apk --release
```

APK файл будет создан в:
```
build/app/outputs/flutter-apk/app-release.apk
```

#### Шаг 2: Установка APK на устройство

**Способ 1: Через USB**
```bash
# Установить APK на подключенное устройство
adb install build/app/outputs/flutter-apk/app-release.apk
```

**Способ 2: Через файловый менеджер**
1. Скопируйте APK файл на Android устройство (через USB, email, облако)
2. На устройстве откройте файловый менеджер
3. Найдите APK файл и нажмите на него
4. Разрешите установку из неизвестных источников (если нужно)
5. Нажмите "Установить"

**Способ 3: Через Google Drive / Dropbox**
1. Загрузите APK в облачное хранилище
2. Откройте на устройстве через приложение облачного хранилища
3. Скачайте и установите

### Вариант 3: Создание App Bundle для Google Play

```bash
# Создать App Bundle для публикации в Google Play
flutter build appbundle --release
```

Файл будет создан в:
```
build/app/outputs/bundle/release/app-release.aab
```

---

## 🍎 Установка на iPhone

### Требования для iOS:
- **Mac компьютер** (обязательно)
- **Xcode** установлен (из App Store)
- **Apple Developer аккаунт** (бесплатный для разработки, $99/год для публикации)
- **iPhone** подключен через USB

### Вариант 1: Установка через Xcode (для разработки)

#### Шаг 1: Настройка Xcode
1. Откройте Xcode
2. Войдите в Apple ID:
   - **Xcode** → **Preferences** → **Accounts**
   - Нажмите **+** и добавьте ваш Apple ID
   - Выберите команду (Team)

#### Шаг 2: Настройка проекта
1. Откройте терминал и перейдите в папку проекта:
   ```bash
   cd /Users/vladkharin/my_chat_app
   ```

2. Откройте iOS проект в Xcode:
   ```bash
   open ios/Runner.xcworkspace
   ```

3. В Xcode:
   - Выберите **Runner** в навигаторе слева
   - Перейдите на вкладку **Signing & Capabilities**
   - Выберите ваш **Team** (Apple ID)
   - Xcode автоматически создаст Bundle Identifier

#### Шаг 3: Подключение iPhone
1. Подключите iPhone к Mac через USB
2. На iPhone: **Настройки** → **Основные** → **Управление устройством**
3. Доверьтесь компьютеру (если появится)

#### Шаг 4: Сборка и установка
1. В Xcode выберите ваше устройство в верхней панели
2. Нажмите **▶️ Play** или `Cmd + R`
3. На iPhone появится запрос на доверие разработчику:
   - **Настройки** → **Основные** → **VPN и управление устройством**
   - Найдите ваш профиль разработчика
   - Нажмите **Доверять**

### Вариант 2: Установка через Flutter CLI

```bash
# Перейдите в папку проекта
cd /Users/vladkharin/my_chat_app

# Установите зависимости
flutter pub get

# Откройте iOS проект в Xcode (для настройки подписи)
open ios/Runner.xcworkspace

# После настройки подписи в Xcode, запустите:
flutter run
```

### Вариант 3: Создание IPA для TestFlight / App Store

#### Шаг 1: Настройка подписи в Xcode
1. Откройте `ios/Runner.xcworkspace` в Xcode
2. Выберите **Runner** → **Signing & Capabilities**
3. Выберите ваш **Team** с платным аккаунтом ($99/год)
4. Убедитесь, что **Automatically manage signing** включено

#### Шаг 2: Создание архива
1. В Xcode выберите **Product** → **Archive**
2. Дождитесь завершения архивации
3. Откроется окно **Organizer**

#### Шаг 3: Загрузка в App Store Connect
1. В окне **Organizer** выберите архив
2. Нажмите **Distribute App**
3. Выберите **App Store Connect**
4. Следуйте инструкциям

#### Шаг 4: Установка через TestFlight
1. Загрузите приложение в App Store Connect
2. Добавьте тестировщиков в TestFlight
3. Тестировщики получат приглашение по email
4. Установите TestFlight из App Store
5. Откройте приглашение и установите приложение

### Вариант 4: Прямая установка IPA (для разработки)

```bash
# Создать IPA для разработки
flutter build ipa --debug

# Или для релиза
flutter build ipa --release
```

IPA файл будет создан в:
```
build/ios/ipa/
```

Для установки IPA нужны специальные инструменты или TestFlight.

---

## 🔧 Решение проблем

### Android

**Проблема: Устройство не определяется**
```bash
# Проверьте подключение
adb devices

# Если устройство не видно, попробуйте:
adb kill-server
adb start-server
adb devices
```

**Проблема: Ошибка при установке APK**
- Убедитесь, что включена установка из неизвестных источников
- Проверьте, что APK не поврежден
- Попробуйте удалить старое приложение перед установкой

### iOS

**Проблема: "Untrusted Developer"**
1. На iPhone: **Настройки** → **Основные** → **VPN и управление устройством**
2. Найдите ваш профиль разработчика
3. Нажмите **Доверять**

**Проблема: Ошибка подписи**
- Убедитесь, что выбран правильный Team в Xcode
- Проверьте, что Bundle Identifier уникален
- Попробуйте очистить проект: `flutter clean`

**Проблема: "No devices found"**
- Убедитесь, что iPhone разблокирован
- Проверьте, что доверили компьютеру на iPhone
- Перезапустите Xcode

---

## 📦 Публикация в магазинах

### Google Play Store

1. **Создайте аккаунт разработчика** ($25 единоразово)
2. **Создайте App Bundle**:
   ```bash
   flutter build appbundle --release
   ```
3. Загрузите в **Google Play Console**
4. Заполните информацию о приложении
5. Отправьте на проверку

### Apple App Store

1. **Создайте аккаунт разработчика** ($99/год)
2. **Создайте архив** в Xcode
3. Загрузите в **App Store Connect**
4. Заполните информацию о приложении
5. Отправьте на проверку (может занять несколько дней)

---

## 🚀 Быстрый старт

### Android (самый простой способ):
```bash
cd /Users/vladkharin/my_chat_app
flutter pub get
flutter run
```

### iOS (требует Mac):
```bash
cd /Users/vladkharin/my_chat_app
flutter pub get
open ios/Runner.xcworkspace
# Настройте подпись в Xcode, затем:
flutter run
```

---

## 📝 Полезные команды

```bash
# Проверить подключенные устройства
flutter devices

# Очистить проект
flutter clean

# Обновить зависимости
flutter pub get

# Проверить настройки Flutter
flutter doctor

# Собрать для Android
flutter build apk --release

# Собрать для iOS
flutter build ios --release
```

---

## ⚠️ Важные замечания

1. **Для iOS нужен Mac** - без Mac нельзя собрать приложение для iPhone
2. **Apple Developer аккаунт** - бесплатный для разработки на своем устройстве, платный для публикации
3. **Google Play** - единоразовый платеж $25 для публикации
4. **Безопасность** - не распространяйте debug версии, используйте release

---

## 🆘 Нужна помощь?

Если возникли проблемы:
1. Проверьте `flutter doctor` - покажет проблемы с настройкой
2. Проверьте логи в консоли
3. Убедитесь, что все зависимости установлены: `flutter pub get`

