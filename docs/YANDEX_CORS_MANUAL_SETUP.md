# 🔧 Ручная настройка CORS в Яндекс Object Storage

## Правильный формат для формы

Если автоматическая настройка не работает, заполните форму вручную:

### Вариант 1: Простой (рекомендуется)

**Описание:**
```
Разрешить доступ к изображениям
```

**Allowed Origins*** (обязательно):
```
*
```

**Allowed Methods*** (обязательно):
```
GET
HEAD
OPTIONS
```
*Каждый метод на новой строке или через запятую*

**Allowed Headers:**
```
*
```

**Expose Headers:**
```
ETag
Content-Length
Content-Type
```
*Каждый заголовок на новой строке*

**MaxAgeSeconds:**
```
3600
```

### Вариант 2: Если звездочка не работает

**Allowed Origins***:
```
https://my-chat-app-estellias-projects.vercel.app
https://my-chat-app.vercel.app
http://localhost:3000
http://localhost:8080
```
*Каждый домен на новой строке*

**Allowed Methods***:
```
GET
HEAD
OPTIONS
```
*Каждый метод на новой строке*

**Allowed Headers:**
```
*
```

**Expose Headers:**
```
ETag
Content-Length
Content-Type
```

**MaxAgeSeconds:**
```
3600
```

## Важно

1. **Allowed Origins** и **Allowed Methods** - обязательные поля (помечены *)
2. Если используете `*` для Allowed Origins, убедитесь, что это одна строка
3. Методы можно указать через запятую: `GET, HEAD, OPTIONS` или каждый на новой строке
4. Заголовки можно указать через запятую или каждый на новой строке

## Если ошибка сохраняется

Попробуйте:
1. Указать методы через запятую в одной строке: `GET, HEAD, OPTIONS`
2. Указать конкретные домены вместо `*`
3. Оставить Expose Headers пустым
4. Установить MaxAgeSeconds в `0` (без кеширования)

## Альтернатива: Использовать автоматическую настройку

Вместо ручной настройки, можно использовать автоматическую:

1. Закоммитьте изменения (скрипт уже создан)
2. После деплоя сервер автоматически настроит CORS
3. Проверьте логи на Render.com - должно быть: `✅ CORS настроен автоматически`

