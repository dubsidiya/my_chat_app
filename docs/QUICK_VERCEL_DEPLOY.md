# 🚀 Быстрый деплой на Vercel

## Пошаговая инструкция (5 минут)

### 1. Подготовка GitHub репозитория

```bash
# Если еще не создан Git репозиторий
git init
git add .
git commit -m "Ready for Vercel deployment"

# Создайте репозиторий на GitHub и загрузите код
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### 2. Деплой на Vercel

#### Через веб-интерфейс (рекомендуется):

1. **Откройте [vercel.com](https://vercel.com)**
2. **Войдите через GitHub**
3. **Нажмите "Add New Project"**
4. **Выберите ваш репозиторий**
5. **Настройте проект**:
   - **Framework Preset**: Other
   - **Root Directory**: `.` (корень проекта)
   - **Build Command**: `flutter build web`
   - **Output Directory**: `build/web`
   - **Install Command**: `flutter pub get`
6. **Нажмите "Deploy"**

#### Через CLI:

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите
vercel login

# Деплой
cd /Users/vladkharin/my_chat_app
vercel

# Для продакшена
vercel --prod
```

### 3. Готово! 🎉

После деплоя получите ссылку вида:
`https://ваш-проект.vercel.app`

---

## ⚙️ Настройки проекта

Файл `vercel.json` уже настроен правильно:
- ✅ Build команда: `flutter build web`
- ✅ Output директория: `build/web`
- ✅ Rewrites для SPA

---

## 🔄 Автоматический деплой

После первого деплоя:
- ✅ Каждый push в `main` → автоматический деплой
- ✅ Pull Request → preview деплой
- ✅ Все деплои в истории проекта

---

## 📝 Важно

1. **Убедитесь, что сервер на Render запущен**
2. **Проверьте, что база данных доступна**
3. **После деплоя протестируйте все функции**

---

## 🐛 Если что-то не работает

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что Build Command правильный
3. Проверьте, что Output Directory указан верно

---

## ✅ Готово!

Теперь ваше приложение доступно по ссылке Vercel!
Поделитесь ссылкой с другими пользователями - они смогут открыть в браузере.

