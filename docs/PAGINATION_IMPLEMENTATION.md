# 📄 Реализация пагинации сообщений

## ✅ Что реализовано

### 1. Клиентская часть (Flutter)

#### Основные компоненты:
- ✅ **Автоматическая подгрузка** при скролле вверх (к старым сообщениям)
- ✅ **Индикатор загрузки** при подгрузке сообщений
- ✅ **Кнопка "Загрузить еще"** для ручной загрузки
- ✅ **Сохранение позиции скролла** при подгрузке новых сообщений
- ✅ **Обработка reverse списка** (новые сообщения внизу)

#### Параметры:
- `_messagesPerPage = 50` - количество сообщений на страницу
- Автоматическая подгрузка при скролле до 300px от верха

#### Логика работы:

1. **Первая загрузка:**
   - Загружаются последние 50 сообщений
   - Список прокручивается вниз (к новым сообщениям)

2. **Подгрузка старых сообщений:**
   - При скролле вверх (к старым сообщениям) автоматически загружаются еще 50
   - Позиция скролла сохраняется (пользователь не теряет место)
   - Используется cursor-based пагинация (по ID сообщения)

3. **Индикаторы:**
   - Показывается индикатор загрузки при подгрузке
   - Показывается кнопка "Загрузить еще" если есть еще сообщения

### 2. Серверная часть (Node.js)

#### Поддержка двух типов пагинации:

1. **Cursor-based (рекомендуется):**
   - Используется при подгрузке старых сообщений
   - Параметр: `beforeMessageId` - ID сообщения, до которого загружать
   - Более эффективна для больших объемов данных

2. **Offset-based:**
   - Используется при первой загрузке
   - Параметры: `limit` и `offset`
   - Проще в реализации

#### API Endpoint:

```
GET /messages/:chatId?limit=50&offset=0&before=123
```

**Параметры:**
- `limit` (опционально, по умолчанию 50) - количество сообщений
- `offset` (опционально, по умолчанию 0) - смещение для offset-based
- `before` (опционально) - ID сообщения для cursor-based пагинации

**Ответ:**
```json
{
  "messages": [
    {
      "id": 123,
      "chat_id": 1,
      "user_id": 1,
      "content": "Текст сообщения",
      "created_at": "2024-01-01T12:00:00Z",
      "sender_email": "user@example.com"
    }
  ],
  "pagination": {
    "hasMore": true,
    "totalCount": 150,
    "limit": 50,
    "offset": 0,
    "oldestMessageId": 123
  }
}
```

---

## 🔧 Технические детали

### Логика скролла (reverse: true)

В Flutter при использовании `reverse: true`:
- `minScrollExtent = 0` - верх списка (старые сообщения)
- `maxScrollExtent` - низ списка (новые сообщения)

Поэтому проверка для подгрузки:
```dart
if (_scrollController.position.pixels <= 300) {
  // Прокрутили почти до верха - загружаем старые сообщения
  _loadMoreMessages();
}
```

### Восстановление позиции скролла

При добавлении новых сообщений в начало списка:
1. Сохраняем текущую позицию скролла
2. Сохраняем максимальную высоту контента (`maxScrollExtent`)
3. Добавляем сообщения
4. Вычисляем разницу в высоте контента
5. Корректируем позицию скролла на эту разницу

```dart
final heightDifference = maxScrollExtentAfter - maxScrollExtentBefore;
final newScrollPosition = currentScrollPosition + heightDifference;
_scrollController.jumpTo(newScrollPosition);
```

---

## 📊 Производительность

### Оптимизации:

1. **Загрузка по частям:**
   - Загружаются только 50 сообщений за раз
   - Снижает нагрузку на сеть и память

2. **Cursor-based пагинация:**
   - Более эффективна чем offset-based
   - Не зависит от общего количества сообщений

3. **Удаление дубликатов:**
   - Проверка на дубликаты при добавлении сообщений
   - Избегает повторного отображения

### Рекомендации по индексам БД:

Для оптимизации запросов рекомендуется создать индексы:

```sql
-- Индекс для быстрого поиска сообщений по чату и времени
CREATE INDEX IF NOT EXISTS idx_messages_chat_created 
ON messages(chat_id, created_at DESC);

-- Индекс для cursor-based пагинации
CREATE INDEX IF NOT EXISTS idx_messages_chat_id 
ON messages(chat_id, id DESC);
```

---

## 🐛 Известные ограничения

1. **Оценка высоты сообщений:**
   - Используется фиксированная оценка для восстановления позиции
   - Может быть неточной для сообщений разной длины

2. **WebSocket и пагинация:**
   - Новые сообщения через WebSocket добавляются в конец списка
   - Не влияют на пагинацию старых сообщений

3. **Большие сообщения:**
   - Очень длинные сообщения могут влиять на точность восстановления позиции

---

## 🚀 Будущие улучшения

1. **Виртуализация списка:**
   - Использование `ListView.builder` с виртуализацией (уже используется)
   - Можно добавить `AutomaticKeepAliveClientMixin` для кэширования

2. **Кэширование:**
   - Кэширование загруженных сообщений
   - Офлайн поддержка

3. **Умная загрузка:**
   - Предзагрузка при приближении к границе
   - Адаптивный размер страницы в зависимости от скорости сети

4. **Точное восстановление позиции:**
   - Использование `ScrollController.position.pixels` с учетом реальной высоты элементов
   - Сохранение ключей виджетов для точного позиционирования

---

## 📝 Использование

### Для пользователя:

1. Откройте чат - загрузятся последние 50 сообщений
2. Прокрутите вверх - автоматически загрузятся старые сообщения
3. Или нажмите кнопку "Загрузить старые сообщения" вверху списка

### Для разработчика:

```dart
// Загрузка сообщений с пагинацией
final result = await _messagesService.fetchMessagesPaginated(
  chatId,
  limit: 50,
  beforeMessageId: oldestMessageId, // Для подгрузки старых
);

// Проверка наличия еще сообщений
if (result.hasMore) {
  // Можно загрузить еще
}
```

---

*Реализовано: 2024*

