# Как запушить проект в GitHub

## ⚠️ Важно: Папка build НЕ должна быть в репозитории!

Папка `build/` находится в `.gitignore` и это правильно - Vercel сам соберет проект при деплое.

---

## 🚀 Способ 1: Через Android Studio

### Шаг 1: Настройте Git в Android Studio

1. **Откройте Android Studio**
2. **VCS → Enable Version Control Integration**
3. **Выберите Git**

### Шаг 2: Добавьте файлы в Git

1. **Правой кнопкой на папку проекта** → **Git → Add**
2. **Или**: VCS → Git → Add

### Шаг 3: Создайте коммит

1. **VCS → Commit**
2. **Напишите сообщение**: "Initial commit" или "Ready for Vercel"
3. **Нажмите Commit**

### Шаг 4: Загрузите на GitHub

1. **VCS → Git → Push**
2. **Если репозиторий еще не создан**:
   - Нажмите "Define remote"
   - URL: `https://github.com/ваш-username/ваш-репозиторий.git`
   - Создайте репозиторий на GitHub сначала
3. **Нажмите Push**

---

## 💻 Способ 2: Через терминал (проще)

### Шаг 1: Инициализируйте Git (если еще не сделано)

```bash
cd /Users/vladkharin/my_chat_app
git init
```

### Шаг 2: Добавьте файлы

```bash
git add .
```

### Шаг 3: Создайте коммит

```bash
git commit -m "Ready for Vercel deployment"
```

### Шаг 4: Создайте репозиторий на GitHub

1. Откройте [github.com](https://github.com)
2. Нажмите "New repository"
3. Назовите репозиторий (например: `my-chat-app`)
4. **НЕ** добавляйте README, .gitignore или лицензию
5. Нажмите "Create repository"

### Шаг 5: Подключите и запушьте

```bash
# Замените на ваш репозиторий
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

---

## ✅ Что будет загружено:

- ✅ Весь код приложения (`lib/`)
- ✅ Конфигурационные файлы (`pubspec.yaml`, `vercel.json`)
- ✅ Документация (`.md` файлы)
- ❌ Папка `build/` (игнорируется - это нормально!)
- ❌ Папка `node_modules/` (если есть)
- ❌ Другие файлы из `.gitignore`

---

## 🔍 Проверка перед пушем

```bash
# Посмотрите, что будет закоммичено
git status

# Убедитесь, что build/ не в списке
git status | grep build
# (ничего не должно вывестись)
```

---

## ⚠️ Если Android Studio показывает ошибки:

1. **Убедитесь, что Git установлен**:
   ```bash
   git --version
   ```

2. **Настройте Git в Android Studio**:
   - File → Settings → Version Control → Git
   - Укажите путь к Git: `/usr/bin/git` или `/usr/local/bin/git`

3. **Используйте терминал** - это проще и надежнее

---

## 📝 После успешного пуша:

1. Откройте ваш репозиторий на GitHub
2. Убедитесь, что файлы загружены
3. Переходите к деплою на Vercel (см. `QUICK_VERCEL_DEPLOY.md`)

---

## 🎯 Быстрая команда (скопируйте и выполните):

```bash
cd /Users/vladkharin/my_chat_app
git init
git add .
git commit -m "Ready for Vercel"
# Затем создайте репозиторий на GitHub и выполните:
# git remote add origin https://github.com/ваш-username/репозиторий.git
# git push -u origin main
```

