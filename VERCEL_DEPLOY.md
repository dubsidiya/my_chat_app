# Деплой на Vercel - Пошаговая инструкция

## 🚀 Быстрый деплой через Vercel Dashboard

### Шаг 1: Подготовка проекта

1. **Убедитесь, что проект собран**:
   ```bash
   flutter build web
   ```

2. **Создайте Git репозиторий** (если еще не создан):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. **Загрузите на GitHub**:
   - Создайте репозиторий на GitHub
   - Загрузите код:
   ```bash
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

### Шаг 2: Деплой на Vercel

#### Вариант A: Через веб-интерфейс (самый простой)

1. **Зарегистрируйтесь на Vercel**:
   - Перейдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub

2. **Создайте новый проект**:
   - Нажмите "Add New Project"
   - Выберите ваш GitHub репозиторий
   - Vercel автоматически определит Flutter проект

3. **Настройте проект**:
   - **Framework Preset**: Other
   - **Build Command**: `flutter build web`
   - **Output Directory**: `build/web`
   - **Install Command**: `flutter pub get`

4. **Нажмите "Deploy"**

5. **Готово!** Получите ссылку вида: `https://ваш-проект.vercel.app`

#### Вариант B: Через Vercel CLI

1. **Установите Vercel CLI**:
   ```bash
   npm install -g vercel
   ```

2. **Войдите в Vercel**:
   ```bash
   vercel login
   ```

3. **Деплой**:
   ```bash
   cd /Users/vladkharin/my_chat_app
   vercel
   ```

4. **Следуйте инструкциям**:
   - Выберите проект или создайте новый
   - Подтвердите настройки
   - Дождитесь завершения деплоя

5. **Для продакшена**:
   ```bash
   vercel --prod
   ```

---

## ⚙️ Настройка проекта

### Файл vercel.json

Уже создан файл `vercel.json` с правильными настройками:
- Build команда: `flutter build web`
- Output директория: `build/web`
- Rewrites для SPA (Single Page Application)

### Переменные окружения (если нужно)

Если нужно изменить URL сервера, добавьте в настройках Vercel:
- **Environment Variables** → Add:
  - `API_URL` = `https://my-server-chat.onrender.com`

---

## 🔄 Автоматический деплой

После первого деплоя:
- Каждый push в `main` ветку автоматически деплоится
- Pull Request создают preview деплои
- Все деплои доступны в истории проекта

---

## 📝 Чеклист перед деплоем

- [ ] Проект собран: `flutter build web`
- [ ] Код загружен на GitHub
- [ ] Создан аккаунт на Vercel
- [ ] Проект подключен к Vercel
- [ ] Настройки проекта правильные:
  - Build Command: `flutter build web`
  - Output Directory: `build/web`
- [ ] Деплой успешно завершен
- [ ] Приложение работает по ссылке

---

## 🐛 Решение проблем

### Проблема: Build fails

**Решение**:
1. Убедитесь, что Flutter установлен в системе сборки Vercel
2. Добавьте в `vercel.json`:
   ```json
   {
     "installCommand": "flutter pub get"
   }
   ```

### Проблема: 404 ошибки при навигации

**Решение**:
- Убедитесь, что в `vercel.json` есть rewrites для SPA
- Файл уже настроен правильно

### Проблема: WebSocket не работает

**Решение**:
- Убедитесь, что сервер на Render поддерживает WebSocket
- Проверьте, что URL сервера правильный в коде

---

## 🔗 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Flutter Web Deployment](https://docs.flutter.dev/deployment/web)
- [Vercel CLI](https://vercel.com/docs/cli)

---

## ✅ После деплоя

1. **Проверьте работу приложения**:
   - Откройте ссылку Vercel
   - Проверьте все функции

2. **Настройте домен** (опционально):
   - В настройках проекта добавьте свой домен
   - Настройте DNS записи

3. **Поделитесь ссылкой**:
   - Другие пользователи могут открыть приложение в браузере
   - Ничего устанавливать не нужно!

---

## 🎉 Готово!

После деплоя ваше приложение будет доступно по ссылке вида:
`https://ваш-проект.vercel.app`

Все пользователи смогут открыть его в любом браузере!

