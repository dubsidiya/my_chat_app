# Решение проблемы с Flutter на Vercel

## Проблема
Vercel не имеет Flutter в своей среде сборки по умолчанию.

## ✅ Решение 1: GitHub Actions + Vercel (Рекомендуется)

### Шаг 1: Соберите локально и загрузите build/web

```bash
cd /Users/vladkharin/my_chat_app
flutter build web --release
```

### Шаг 2: Измените vercel.json

Измените `vercel.json` чтобы использовать уже собранные файлы:

```json
{
  "version": 2,
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### Шаг 3: Загрузите build/web в репозиторий

Создайте отдельную ветку или добавьте build/web в .gitignore исключение:

```bash
# Временно разрешим build/web
echo "!build/web/" >> .gitignore

# Добавим build/web
git add build/web
git commit -m "Add built web files"
git push
```

### Шаг 4: Настройте Vercel

В настройках проекта Vercel:
- **Build Command**: оставьте пустым или `echo "Already built"`
- **Output Directory**: `build/web`
- **Install Command**: оставьте пустым

---

## ✅ Решение 2: Использовать Netlify (проще для Flutter)

Netlify лучше поддерживает Flutter:

1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Подключите GitHub репозиторий
3. Настройки:
   - **Build command**: `flutter build web`
   - **Publish directory**: `build/web`
4. Добавьте переменную окружения:
   - `FLUTTER_VERSION`: `3.24.0`

---

## ✅ Решение 3: GitHub Actions для автоматической сборки

Используйте GitHub Actions для сборки и деплоя (файл уже создан).

### Настройка:

1. Получите токены Vercel:
   - Зайдите на [vercel.com/account/tokens](https://vercel.com/account/tokens)
   - Создайте токен
   - Скопируйте `VERCEL_TOKEN`

2. Получите ID проекта и организации:
   - В настройках проекта Vercel найдите `Project ID` и `Org ID`

3. Добавьте секреты в GitHub:
   - Откройте репозиторий → Settings → Secrets and variables → Actions
   - Добавьте:
     - `VERCEL_TOKEN`
     - `VERCEL_ORG_ID`
     - `VERCEL_PROJECT_ID`

4. GitHub Actions автоматически соберет и задеплоит при каждом push

---

## ✅ Решение 4: Собрать локально и загрузить на Vercel вручную

1. **Соберите локально**:
   ```bash
   flutter build web --release
   ```

2. **Загрузите через Vercel CLI**:
   ```bash
   npm install -g vercel
   vercel login
   cd build/web
   vercel --prod
   ```

---

## 🎯 Рекомендация

**Самый простой вариант**: Используйте **Netlify** вместо Vercel - он лучше поддерживает Flutter из коробки.

Или используйте **Решение 4** - соберите локально и загрузите build/web напрямую.

