# GitHub Actions отключен - используем встроенную интеграцию Vercel с GitHub
# Vercel автоматически деплоит при push в main, используя файлы из build/web/
#
# Если хотите использовать GitHub Actions вместо интеграции Vercel:
# 1. Добавьте секреты в GitHub: Settings -> Secrets and variables -> Actions
#    - VERCEL_TOKEN (из Vercel: Settings -> Tokens)
#    - VERCEL_ORG_ID (из Vercel: Settings -> General)
#    - VERCEL_PROJECT_ID (из настроек проекта Vercel)
# 2. Раскомментируйте код ниже

# name: Build and Deploy to Vercel
#
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#         
#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: '3.24.0'
#           channel: 'stable'
#       
#       - name: Install dependencies
#         run: flutter pub get
#       
#       - name: Build web
#         run: flutter build web --release
#       
#       - name: Deploy to Vercel
#         uses: amondnet/vercel-action@v25
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#           vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#           working-directory: ./
#           scope: ${{ secrets.VERCEL_ORG_ID }}

